Ок, сформулирую **архитектуру SpecRails** на уровне системного дизайна, так чтобы она была:

* минималистичной
* масштабируемой
* понятной разработчикам
* удобной для аналитиков
* пригодной для расширения (но не перегруженной)

---

# 🏗 **SpecRails — Архитектура**

## 🎯 Назначение системы

SpecRails — инструмент, который помогает аналитикам:

* писать спецификации в человеческом виде (естественный язык)
* получать из них структурированные артефакты (формы, модели, ACL, FSM)
* просматривать формы и логику во lightweight preview
* использовать spec-файлы как **исходный код требований**

SpecRails **не конструктор UI** и не AI-IDE.
Это **утилита для структурирования мысли и проектной информации**.

---

## 🧩 Высокоуровневые компоненты

```
+--------------------------------------------------+
|            User Workspace (project files)        |
|   - markdown docs                                |
|   - *.form.yaml / *.model.yaml / *.acl.yaml      |
|   - spec includes / components                   |
+---------------------↑----------------------------+
                      |
                      | Spec files (source of truth)
                      |
+---------------------↓----------------------------+
|             SpecRails Core (runtime)            |
|                                                  |
|  Parsing   → YAML → Spec internal model          |
|  Validation→ JSON-schema                         |
|  Composition→ include/compose engine             |
|  Abstract UI model (field types, rules)          |
|  Transformation API                              |
|                                                  |
|  (no model training, no LLM inside)              |
+---------------------↑----------------------------+
                      |
                      | Request (user intent)
                      |
+---------------------↓----------------------------+
|           SpecRails AI Layer (adapters)         |
|  - prompts                                      |
|  - role instruction                             |
|  - output contract enforcement                  |
|  - result normalization                         |
|  - anti-hallucination filters                   |
|                                                  |
|  AI providers:                                  |
|   - OpenAI                                      |
|   - Anthropic                                   |
|   - Local (later optional)                      |
+---------------------↑----------------------------+
                      |
                      | result (spec text / diff)
                      |
+---------------------↓----------------------------+
|            Editor Integrations                  |
|  - VS Code extension                            |
|  - Chat slash-commands (later)                  |
|  - CLI (optional future)                        |
|                                                  |
|  Features:                                      |
|   - slash actions (/form /model /roles /struct) |
|   - “Preview form” panel                        |
|   - inline AI ask panel                         |
+--------------------------------------------------+
```

---

## ⚙️ Ключевая логика

### **1. Истина — в spec-файлах**

Никаких скрытых структур.
Все артефакты — обычные файлы рядом с текстом аналитика.

### **2. AI — преобразователь текста → spec**

AI **не владеет проектом**, он помощник.

### **3. Preview — HTML + мини-runtime**

* генерируемая форма ≠ production UI
* только для проверки логики

### **4. UI-уровень — абстрактные элементы**

(по типу Element-Plus, но без зависимости)

Mapping:

```
Input     → <input type=text>
Select    → <select>
Checkbox  → <input type=checkbox>
Date      → <input type=date>
```

---

## 🧠 Принципы архитектуры

| Принцип                      | Что значит                                      |
| ---------------------------- | ----------------------------------------------- |
| Минимализм                   | только нужные функции                           |
| Файлы = Истина               | никакого скрытого состояния                     |
| AI как периферия             | не ядро, а plug-in                              |
| DSL > UI                     | спецификации — основной продукт                 |
| Preview лёгкий               | HTML, JS runtime, без фреймворков               |
| Расширяемость через адаптеры | Vue/React optional, потом                       |
| Без магии                    | читаемость и предсказуемость важнее возможности |

---

## 📦 Модули

### `@specrails/core`

* парсер
* спецификаторы типов
* JSON-схемы валидаторов
* include/compose engine
* рендер π (abstract → HTML DOM)

### `@specrails/ai`

* prompting contracts
* error handling
* adapters для моделей

### `@specrails/vscode`

* команды
* UI для preview
* inline AI call UI

### (позже) `@specrails/vue-adapter`

* маппинг абстрактных контролов → реальных Vue компонентов

---

## 🛠 Поток работы

### 1. Аналитик пишет текст

```
Пользователь должен заполнить профиль...
```

### 2. Выделяет и вызывает команду

`/form`

### 3. SpecRails AI → YAML spec

```yaml
fields:
  - id: firstName
    type: Input
```

### 4. SpecRails core валидирует

* JSON schema OK
* required параметры OK

### 5. Preview

HTML-страница + 60 строк JS

### 6. Аналитик правит — SpecRails не мешает

---

## 🎁 Ключевые особенности

* “AI не думает за тебя — он структурирует твою мысль”
* “Spec не рождается из магии — он оформляется человеком”
* “Предпросмотр — вспомогательный, а не production UI”
* “Нет скрытой метамодели — только файлы”
* “Расширяемость через адаптеры, не через плагины”

---

## 🧩 Интеграция с UI-китами (позже)

```
.type: Select → adapter(element-plus) → <el-select>
.type: Select → adapter(antd) → <a-select>
.type: Select → adapter(html) → <select>
```

Т.е. мы создаём **UI-агностичный DSL**.

---

## 🧱 Минимальный MVP

Прямо сейчас в планах:

✅ YAML spec
✅ controls v1
✅ include/compose
✅ select options inline
✅ demo values
✅ HTML preview runtime
✅ VS Code extension
✅ AI-slash commands

---

## 🎓 Очень коротко

> SpecRails — файловый DSL + AI-трансформер + лёгкий форм-preview.
> Не UI-framework. Не low-code. Не workflow-платформа.
> Инструмент мышления для аналитика.

---

Хочешь, следующим шагом оформлю это в:

* `SpecRails_Architecture.md`
* диаграммы C4 (context, container, component)
* README architecture section
* roadmap for implementation

Скажи формат:
`md`, `diagrams`, `roadmap`, или `всё сразу`.
