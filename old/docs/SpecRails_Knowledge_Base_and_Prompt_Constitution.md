# 🧠 **SpecRails Knowledge Base & Prompt Constitution**

## 🎯 Цель

> Создать единый слой знаний и принципов (Prompt Constitution), обеспечивающий когнитивную согласованность AI-агентов SpecRails.
> Этот слой задаёт мировоззрение системы: её стиль мышления, приоритеты, ограничения и контекст для генерации DSL, форм и спецификаций.

---

## 🧩 1. Архитектурная роль

| Компонент                    | Назначение                                                            |
| ---------------------------- | --------------------------------------------------------------------- |
| **Knowledge Base (KB)**      | Централизованное хранилище знаний, инструкций, схем и примеров для AI |
| **Prompt Constitution**      | Набор базовых принципов и директив, определяющих поведение AI         |
| **Context Engine**           | Механизм сборки контекста из KB для каждого вызова AI                 |
| **Template Library**         | Репозиторий готовых шаблонов промтов и спецификаций                   |
| **Knowledge Update Manager** | Контролирует обновления, версионность и совместимость знаний          |

---

## 📚 2. Основные задачи слоя

1. Обеспечить **единое когнитивное поле** для всех AI-интеракций.
2. Снизить стохастику за счёт общей базы контекста и примеров.
3. Обеспечить возможность **тонкой настройки поведения** без переписывания промтов.
4. Разделить знания на уровни — от фундаментальных принципов до конкретных примеров.
5. Дать аналитикам и архитекторам прозрачный доступ к содержимому, но под управлением Governance Layer.

---

## 🧱 3. Структура Knowledge Base

```yaml
knowledge_base:
  - domain: "ui/forms"
    path: ".ai/knowledge/forms/base.v1.yaml"
    description: "Основные принципы построения форм и элементов интерфейса"
  - domain: "models/data"
    path: ".ai/knowledge/models/base.v1.yaml"
    description: "Схемы данных и их связи"
  - domain: "security/acl"
    path: ".ai/knowledge/security/acl.v1.yaml"
    description: "Примеры и правила генерации ACL, ALS, RLS"
  - domain: "workflow/fsm"
    path: ".ai/knowledge/workflow/fsm.v1.yaml"
    description: "Примеры построения state-machine и статусов"
```

Каждый элемент базы связан с конкретным доменом и версией.

---

## 📜 4. Prompt Constitution

**Prompt Constitution** — это не набор промтов, а философия взаимодействия с AI.
Она описывает:

* **границы поведения** (что можно и нельзя генерировать),
* **приоритеты** (точность > красота, стабильность > оригинальность),
* **тональность** (аналитическая, лаконичная, инженерная),
* **принципы объяснимости** (всё должно быть верифицируемо и документируемо).

### Пример структуры:

```yaml
prompt_constitution:
  version: 1.0
  principles:
    - "AI должен формировать воспроизводимые артефакты, а не тексты."
    - "Всё, что не может быть проверено, не должно быть принято."
    - "AI обязан следовать структуре контракта, даже если контекст неоднозначен."
    - "При сомнениях AI должен запросить уточнение, а не делать предположение."
  tone: "concise, factual, structured"
  ethics:
    - "Запрещено генерировать личные данные или примеры, выходящие за рамки домена."
    - "Вся генерация должна быть воспроизводимой и объяснимой."
```

---

## ⚙️ 5. Context Engine

**Context Engine** — промежуточный слой между Knowledge Base и Prompt Execution Runtime.
Он собирает релевантные фрагменты знаний для конкретного контракта и подготавливает контекст для AI.

```yaml
context_assembly:
  contract: "form.contract.v2"
  context_layers:
    - constitution: "base.principles.v1"
    - knowledge: ["ui/forms", "models/data"]
    - project: "education"
    - custom_overrides: ["app-specific/rules.yaml"]
```

Таким образом, каждый вызов AI имеет предсказуемый, воспроизводимый контекст, не зависящий от случайных данных.

---

## 🧠 6. Уровни знаний

| Уровень          | Описание                                         | Пример                              |
| ---------------- | ------------------------------------------------ | ----------------------------------- |
| **Foundational** | Общие принципы и директивы (Prompt Constitution) | Тон, стиль, приоритеты              |
| **Domain**       | Знания по области (UI, Models, ACL, FSM)         | Правила генерации форм              |
| **Project**      | Знания по проекту                                | Терминология, специфичные сущности  |
| **Local**        | Временные или контекстные данные                 | Пользовательский ввод, история чата |

---

## 🧩 7. Template Library

Хранилище шаблонов промтов и DSL-конфигураций:

* шаблоны типовых форм (entity form, filter form, detail view);
* шаблоны моделей (entity, relation, taxonomy);
* шаблоны ACL и FSM.

```yaml
template:
  id: "form.basic.v1"
  description: "Базовая форма с обязательными полями"
  structure:
    form:
      title: "{{ entity.label }}"
      fields:
        - name: "name"
          type: "string"
        - name: "description"
          type: "text"
```

---

## 🔄 8. Knowledge Update Lifecycle

| Стадия             | Описание                                               |
| ------------------ | ------------------------------------------------------ |
| **Draft**          | Добавление новых знаний (архитектор или AI Governance) |
| **Validate**       | Проверка корректности и совместимости                  |
| **Publish**        | Добавление в Knowledge Base Registry                   |
| **Version Update** | Синхронизация с контрактами и контекстами              |
| **Deprecate**      | Перенос в архив при устаревании                        |

---

## 🧭 9. Архитектурный принцип

> **Знания — это основа предсказуемости.**
> Prompt Constitution делает поведение AI не зависимым от личности пользователя, а от принципов системы.
> Knowledge Base обеспечивает когнитивное единство всех модулей SpecRails, превращая стохастический AI в инженерный инструмент.
