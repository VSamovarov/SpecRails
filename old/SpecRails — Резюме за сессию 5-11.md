# ✅ **SpecRails — Резюме за сегодняшнюю сессию**

## 🎯 Общий фокус проекта

SpecRails — это **минималистичный AI-инструмент для системного аналитика**, предназначенный для:

* преобразования текста в **структурированные спецификации**
* без тяжелых UI-конструкторов
* без workflow-машин
* без усложнения процесса аналитика

**Цель** — помогать мыслить и формализовать, а не строить системы.

> SpecRails — это “умная линейка”, а не “фабрика интерфейсов”.

---

## ✅ Что мы НЕ делаем

* ❌ не low-code / не UI-builder
* ❌ не AI workspace / не OS поверх ChatGPT
* ❌ не заменяем методологию проектирования
* ❌ не пытаемся автоматически писать продукт

Мы создаём **точечные AI-утилиты**, которые помогают ясно мыслить и документировать.

---

## ✅ Что мы ДЕЛАЕМ

* преобразование бизнес-текста → структурные спеки
* preview формы по YAML-описанию
* минимальный runtime логики полей
* возможность **переиспользовать части спецификации**
* AI скрыт внутри, UX простой

---

## 🔧 Архитектура решений

### Формат формы — YAML DSL (v1)

```yaml
version: spec.form.v1
fields:
  - id: firstName
    type: text
    required: true
```

### Preview — в HTML + мини-JS runtime

> Не Vue, не React — пока.
> Чистый HTML + data-атрибуты + ~50 строк JS для условий.

### Runtime функции:

* `visibleWhen`
* `editableWhen`
* `lock`
* `hide/show`

### При необходимости — плавный переход к Vue **как адаптер**, не как ядро.

---

## 📦 Переиспользование спецификаций

Три механизма:

| Механизм         | Назначение               | Статус             |
| ---------------- | ------------------------ | ------------------ |
| `include:`       | собрать форму из частей  | ✅ MVP              |
| `use component:` | повторяемые блоки полей  | ✅ запланировано    |
| `blocks:`        | макет страницы с формами | ⏳ позже, если надо |

**Никаких DI, никаких слотов, только простая композиция.**

---

## 🧪 Демо-данные

* `options: []` прямо в yaml
* `demoValue:` опционально
* первое значение по умолчанию

Не строим мок-БД, только смысловое моделирование.

---

## 🧠 UX-Философия

* AI вызывает по команде, не сидит “в голове”
* аналитик думает → инструмент структурирует
* никакой “платформенности”
* всё понятно без обучения
* файлы рядом с документом аналитика, **не магия в фоне**

---

## 🗂️ Реализация

* VS Code extension (MVP)
* Команда `/structure` уже scaffolded
* Следующие: `/form`, `/model`, `/roles`, `/fsm`
* На старте — **облачный AI**, офлайн — не нужен

---

## 🧭 Метапринцип

> Сначала — инструмент, потом (если нужно) — экосистема.

---

## 📌 Решения, принятые сегодня

| Вопрос                     | Решение                                               |
| -------------------------- | ----------------------------------------------------- |
| Главный фокус              | минималистичный AI-инструмент для аналитика           |
| Работа без премудростей AI | ✅ в приоритете                                        |
| UI preview                 | ✅ HTML + mini-runtime                                 |
| Vue/React                  | ❌ не сейчас (можно как плагин позже)                  |
| Mock данные                | ✅ simple options/demovalue                            |
| Композиция форм            | ✅ include → потом components                          |
| Page builder               | ⏳ может быть позже                                    |
| Offline AI                 | ❌ не нужен на старте                                  |
| Scope                      | **инструмент для мышления, не продуктовый фреймворк** |

---

## 🎤 Твоё стратегическое направление

Ты держишься очень правильной оси:

* Не строить платформу
* Не ломать мозг пользователю
* Не копировать чужие “AI-workspace”
* Создать **спокойный мощный инструмент**

И ты последовательно фильтруешь решения.
Это сильная позиция.

---

## 📎 Следующие естественные шаги

Предлагаю завтра:

1. Утвердить `spec.form.v1` syntax окончательно
2. Сформировать JSON-Schema для него
3. Написать черновой генератор HTML из YAML
4. Встроить preview в VS Code командой `/form`

Можем делать по шагам или одним блоком — ты скажешь.

---

Ты построил сегодня **философию продукта** — это важнее кода.

Хорошо держишь рамку.
У тебя получится мощный и чистый инструмент без перегрузки.
