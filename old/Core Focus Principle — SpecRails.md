Отлично.
Это ключевой момент, который задаёт архитектурную ось всего проекта — **всё остальное должно вращаться вокруг ядра DSL**.

Вот как я бы оформил эту идею как стратегическую установку SpecRails:

---

# ⚙️ **Core Focus Principle — SpecRails**

## 🎯 Архитектурный постулат

> **Ядро SpecRails — это генерация структурированных артефактов (DSL) из текстовых описаний.**
> Всё остальное (preview, AI, методология, интеграции) — лишь инфраструктурное окружение, обеспечивающее надёжность, воспроизводимость и удобство этого процесса.

---

## 🧩 Что входит в ядро

1. **Parser Layer** — преобразование текстовых описаний (user story, narrative, requirement) в структурированные DSL-артефакты (`.form.yaml`, `.model.yaml`, `.acl.yaml`).
2. **Validator Layer** — проверка на синтаксис, целостность и совместимость DSL.
3. **Assembler** — сборка составных спецификаций (include, component, reference).
4. **Spec Schema Registry** — JSON Schema, управляющие DSL-версиями и совместимостью.
5. **Output Contracts** — чётко определённые структуры, пригодные для CI/CD, документации и передачи в разработку.

---

## 🔧 Что *не* является ядром

| Компонент                  | Статус       | Роль                                                               |
| -------------------------- | ------------ | ------------------------------------------------------------------ |
| **AI Layer**               | окружение    | средство генерации исходного текста и проверки, не источник истины |
| **Preview Engine**         | окружение    | визуализация DSL, не часть логики                                  |
| **VS Code Extension**      | интерфейс    | точка входа, не вычислительное ядро                                |
| **Methodology Layer**      | мета-уровень | набор best practices и промт-паттернов, не код                     |
| **UI Renderer (HTML/Vue)** | плагины      | интерпретация DSL, не хранение данных                              |

---

## 🧠 Почему это важно

* Обеспечивает **архитектурную дисциплину** — SpecRails остаётся системой спецификаций, а не “AI workspace”.
* Упрощает сопровождение — ядро не зависит от UI или моделей AI.
* Делает систему **прозрачной и предсказуемой**.
* Позволяет **разделить зоны ответственности**:

  * ядро — инженеры,
  * окружение — AI и UX.

---

## ⚖️ Принцип разделения ответственности

```
      ┌────────────────────────────┐
      │       AI Layer             │
      │ (ввод/контекст/генерация)  │
      └─────────────┬──────────────┘
                    │
                    ▼
      ┌────────────────────────────┐
      │        Core DSL Engine     │
      │ (parse → validate → merge) │
      └─────────────┬──────────────┘
                    │
                    ▼
      ┌────────────────────────────┐
      │     Visualization Layer    │
      │ (preview / validation UI)  │
      └────────────────────────────┘
```

---

## 🚧 Контрольные критерии архитектуры

| Критерий            | Вопрос                                | Требование                          |
| ------------------- | ------------------------------------- | ----------------------------------- |
| **Isolation**       | Может ли ядро работать без AI?        | ✅ Да                                |
| **Reproducibility** | Повторный запуск AI даёт тот же DSL?  | 🔸 Частично, но нормализуется ядром |
| **Replaceability**  | Можно ли сменить AI-провайдера?       | ✅ Да                                |
| **Transparency**    | Понимает ли человек результат без AI? | ✅ Обязательно                       |
| **Extensibility**   | Можно ли добавить новый тип DSL?      | ✅ Через schema registry             |

---

## 🧭 Стратегическое следствие

SpecRails должен восприниматься не как “AI-инструмент”, а как:

> **интерпретатор инженерных мыслей, выраженных текстом.**

AI — лишь один из способов “задать текст”.

---

Если хочешь, я могу оформить это как отдельный документ:
**`SpecRails_Core_Principle.md`** — чтобы он стал частью официальной архитектурной документации и лежал рядом с `SpecRails_Architecture.md`.
Добавить?
