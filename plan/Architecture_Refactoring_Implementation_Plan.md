---
version: 1.0
status: active
prepared_by: AI Architectural Reviewer
date: 2025-11-11
based_on: SpecRails_Architecture_Review_Report-84729.md
scope: Core, Process, Governance layers refactoring
---

# 📋 SpecRails Architecture Refactoring Implementation Plan

## 🎯 Цель плана
Устранить выявленные архитектурные противоречия и формализовать границы модулей для достижения уровня зрелости **Managed (4/5)**.

**Принципы реализации:**
- ✅ YAGNI — только необходимые изменения
- ✅ KISS — концептуальные описания без избыточной детализации
- ✅ Фокус на границах ответственности, не на реализации

---

## 📊 Общая структура плана

```
Этап 1: Формализация границ (Core Layer)          [2-3 дня]
  ↓
Этап 2: Операционализация Validation Loop         [3-4 дня]
  ↓
Этап 3: Замыкание контура Governance              [2-3 дня]
  ↓
Этап 4: Создание Reference Architecture           [1-2 дня]
  ↓
Финальная валидация и ревью                        [1 день]
```

**Общая длительность:** 9-13 рабочих дней

---

## 🔴 Этап 1: Формализация границ (Core Layer)

### Проблема
Parser и Prompt Registry не имеют чёткой границы ответственности. Parser может как создавать DSL, так и регистрировать промпты, что нарушает изоляцию ядра.

### Цель
Создать единственный интерфейс взаимодействия — `PromptManifestDTO`.

### Задачи

#### 1.1 Создать JSON Schema контракта

**Файл:** `c:\Server\SpecRails\schemas\PromptManifestDTO.json`

**Содержание:**
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Prompt Manifest DTO",
  "description": "Контракт передачи данных между Parser и Prompt Registry",
  "type": "object",
  "required": ["name", "version", "contract", "status"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "contract": {
      "type": "string",
      "format": "uri-reference"
    },
    "dependencies": {
      "type": "array",
      "items": { "type": "string" }
    },
    "status": {
      "type": "string",
      "enum": ["draft", "active", "deprecated"]
    },
    "metadata": {
      "type": "object",
      "properties": {
        "author": { "type": "string" },
        "created_at": { "type": "string", "format": "date-time" }
      }
    }
  }
}
```

**Критерий готовности:** ✅ Схема валидна, содержит все необходимые поля

---

#### 1.2 Обновить Terminology_Glossary.md

**Файл:** `c:\Server\SpecRails\docs\Terminology_Glossary.md`

**Добавить термины:**
- `Prompt Manifest DTO` (уже добавлен ✅)
- `Manifest Lifecycle` (уже добавлен ✅)

**Критерий готовности:** ✅ Термины описаны, примеры контрактов присутствуют

---

#### 1.3 Документировать принцип разделения

**Создать новый раздел в документации (концептуально, без создания файла):**

**Принцип:**
```yaml
Parser:
  responsibility: "Создание структуры DSL из текста"
  output: "PromptManifestDTO"
  not_responsible_for: "Регистрация, версионирование, хранение"

Prompt Registry:
  responsibility: "Управление жизненным циклом промптов"
  input: "PromptManifestDTO"
  operations: ["validate", "register", "version", "deprecate"]
  not_responsible_for: "Создание DSL, AI-взаимодействие"
```

**Критерий готовности:** ✅ Граница ответственности явно задокументирована

---

### Результат этапа 1
- ✅ `PromptManifestDTO.json` создан и валиден
- ✅ Термины добавлены в глоссарий
- ✅ Принцип изоляции Parser ↔ Registry зафиксирован

---

## 🔴 Этап 2: Операционализация Validation Loop

### Проблема
Validation Loop описан концептуально, но отсутствует операционная модель: нет явных состояний, правил переходов, ответственных за каждую фазу.

### Цель
Превратить Validation Loop в исполняемую модель с чёткими состояниями и событиями.

### Задачи

#### 2.1 Определить ValidationState enum

**Добавить в Terminology_Glossary.md** (уже добавлено ✅)

**Состояния:**
```yaml
ValidationState:
  - generating    # AI создаёт DSL
  - validating    # Validator проверяет структуру
  - previewing    # Analyst смотрит визуальное представление
  - reviewing     # Analyst оценивает смысл
  - approved      # DSL принят
  - rejected      # требуются исправления
```

**Критерий готовности:** ✅ Enum определён, каждое состояние имеет описание

---

#### 2.2 Описать правила переходов (Transition Rules)

**Концептуальное описание:**

```yaml
Transitions:
  generating → validating:
    trigger: "Parser.complete"
    responsible: "Orchestrator"
  
  validating → previewing:
    trigger: "Validator.pass"
    responsible: "Orchestrator"
  
  validating → rejected:
    trigger: "Validator.fail"
    responsible: "Validator"
  
  previewing → reviewing:
    trigger: "Preview.ready"
    responsible: "UI Layer"
  
  reviewing → approved:
    trigger: "Analyst.approve"
    responsible: "Analyst"
  
  reviewing → rejected:
    trigger: "Analyst.reject"
    responsible: "Analyst"
  
  rejected → generating:
    trigger: "Feedback.apply"
    responsible: "Orchestrator"
```

**Критерий готовности:** ✅ Каждый переход имеет триггер и ответственного

---

#### 2.3 Включить Preview как обязательную UX-фазу

**Обновить описание Validation Loop:**

**Старое:**
```
AI → Validator → Analyst → Feedback → AI
```

**Новое:**
```
AI → Validator → Preview → Analyst Review → [Approved | Feedback] → AI
```

**Принцип:** Preview — это обязательная фаза между формальной и смысловой валидацией.

**Критерий готовности:** ✅ Preview явно включён в документацию цикла

---

#### 2.4 Определить роли агентов

**Добавить в Terminology_Glossary.md** (уже добавлено ✅)

```yaml
Agent Roles:
  Parser:
    phase: "generating"
    input: "текст от аналитика"
    output: "PromptManifestDTO"
  
  Validator:
    phase: "validating"
    input: "PromptManifestDTO"
    output: "ValidationReport"
  
  Analyst:
    phase: "reviewing"
    input: "Preview (визуализация DSL)"
    output: "Decision (approve | reject)"
```

**Критерий готовности:** ✅ Роли описаны, входы и выходы определены

---

#### 2.5 Описать Event Bus концептуально

**Принцип:**
```yaml
Event Bus:
  purpose: "Передача управления между агентами без прямых вызовов"
  
  events:
    - "dsl.generated"
    - "validation.completed"
    - "preview.ready"
    - "review.completed"
  
  consumers:
    Orchestrator: "подписан на все события, управляет состоянием"
    Parser: "публикует dsl.generated"
    Validator: "публикует validation.completed"
```

**Критерий готовности:** ✅ Концепция Event Bus описана без деталей реализации

---

### Результат этапа 2
- ✅ `ValidationState` enum определён
- ✅ Правила переходов задокументированы
- ✅ Preview включён как обязательная фаза
- ✅ Роли агентов описаны
- ✅ Концепция Event Bus зафиксирована

---

## 🟠 Этап 3: Замыкание контура Governance ↔ Observability

### Проблема
Governance и Observability работают изолированно. Observability собирает метрики, но не влияет на политики автоматически.

### Цель
Создать механизм `Policy Hook` для автоматической реакции Governance на метрики.

### Задачи

#### 3.1 Добавить термин Policy Hook

**В Terminology_Glossary.md** (уже добавлено ✅)

```yaml
Policy Hook:
  trigger:
    metric: "drift_index"
    threshold: 0.15
  action:
    type: "require_manual_review"
    notify: ["architect"]
```

**Критерий готовности:** ✅ Термин описан с примером

---

#### 3.2 Описать схему контура обратной связи

**Концептуальная схема:**

```
┌─────────────────┐
│  Observability  │ собирает метрики
└────────┬────────┘
         ↓
┌─────────────────┐
│  Policy Hook    │ проверяет условия (threshold)
└────────┬────────┘
         ↓
┌─────────────────┐
│  Governance     │ применяет действия (require_review)
└────────┬────────┘
         ↓
┌─────────────────┐
│ Validation Loop │ получает ограничения (no auto-merge)
└─────────────────┘
         ↓
    (новые метрики) → обратно в Observability
```

**Критерий готовности:** ✅ Схема понятна, связи явные

---

#### 3.3 Определить примеры триггеров

**Концептуальные примеры:**

```yaml
# Пример 1: Высокий дрейф
PolicyHook_DriftControl:
  trigger:
    metric: "drift_index"
    threshold: 0.15
  action:
    type: "require_manual_review"
    target: "all_contract_changes"
    notify: ["architect", "tech_lead"]

# Пример 2: Частые ошибки валидации
PolicyHook_ValidationQuality:
  trigger:
    metric: "validation_failure_rate"
    threshold: 0.3
    window: "24h"
  action:
    type: "disable_auto_merge"
    notify: ["qa_team"]
```

**Критерий готовности:** ✅ Примеры покрывают основные сценарии

---

### Результат этапа 3
- ✅ Термин `Policy Hook` добавлен
- ✅ Схема контура обратной связи задокументирована
- ✅ Примеры триггеров описаны

---

## 🟢 Этап 4: Создание Reference Architecture

### Проблема
Отсутствует единый документ, описывающий высокоуровневую архитектуру с границами модулей.

### Цель
Создать `Reference_Architecture.md` как источник истины для архитектурных решений.

### Задачи

#### 4.1 Создать новый документ

**Файл:** `c:\Server\SpecRails\docs\Reference_Architecture.md`

**Структура:**
```markdown
# SpecRails Reference Architecture

## Архитектурные принципы
- Изоляция ядра
- Контрактное взаимодействие
- Односторонние зависимости

## Модульные границы
### Core Layer
(ответственность, интерфейсы, ограничения)

### Process Layer
(ответственность, интерфейсы, ограничения)

### Governance Layer
(ответственность, интерфейсы, ограничения)

## Потоки данных
- Создание промпта
- Цикл валидации
- Контур обратной связи

## Принципы безопасности
- Sandbox Context
- Контроль версий

## Критерии архитектурной корректности
```

**Критерий готовности:** ✅ Документ создан, структура соответствует плану

---

#### 4.2 Описать границы модулей

**Для каждого слоя указать:**
- ✅ Что делает (responsibility)
- ✅ Что НЕ делает (not_responsible_for)
- ✅ Входные интерфейсы (input)
- ✅ Выходные интерфейсы (output)

**Критерий готовности:** ✅ Каждый модуль имеет явные границы

---

### Результат этапа 4
- ✅ `Reference_Architecture.md` создан
- ✅ Границы модулей задокументированы
- ✅ Потоки данных описаны

---

## ✅ Финальная валидация

### Проверка метрик завершённости

| Метрика | Цель | Статус |
|---------|------|--------|
| `PromptManifestDTO.json` создан | 1 файл | ⏳ → ✅ |
| Новые термины в Glossary | 6 терминов | ✅ (уже добавлены) |
| ValidationState определён | 6 состояний | ⏳ → ✅ |
| Policy Hook описан | 1 механизм | ✅ (уже добавлен) |
| Reference Architecture создан | 1 документ | ⏳ → ✅ |

---

### Критерии успешного завершения

#### Архитектурная согласованность
- ✅ Ядро изолировано (Core не вызывает Process/Governance)
- ✅ Все передачи данных через DTO
- ✅ Validation Loop операционализирован
- ✅ Governance замкнут с Observability

#### Документация
- ✅ Все термины в глоссарии
- ✅ Reference Architecture создан
- ✅ Improvement Plan синхронизирован

#### Качество
- ✅ Принципы YAGNI и KISS соблюдены
- ✅ Нет избыточных деталей реализации
- ✅ Фокус на границах и контрактах

---

## 📅 График реализации

```
День 1-2:   Этап 1 (PromptManifestDTO, обновление Glossary)
День 3-5:   Этап 2 (ValidationState, правила переходов, роли агентов)
День 6-7:   Этап 3 (Policy Hook, схема контура)
День 8-9:   Этап 4 (Reference Architecture)
День 10:    Финальная валидация, синхронизация документов
```

---

## 🎯 Ожидаемый результат

### До рефакторинга
- Уровень зрелости: **3.5/5 (Defined)**
- Проблемы: размытые границы, нет операционализации, разорванные контуры

### После рефакторинга
- Уровень зрелости: **4.0/5 (Managed)**
- Результат: чёткие интерфейсы, операционная модель, замкнутые контуры

---

## 📝 Следующие шаги после завершения

1. **Провести архитектурное ревью** с использованием обновлённых документов
2. **Обновить DOCUMENTATION_MAP.md** с новыми связями
3. **Создать примеры использования** PromptManifestDTO
4. **Зафиксировать изменения** в репозитории

---

**Статус плана:** Active  
**Готовность к выполнению:** ✅ Да  
**Следующий шаг:** Начать Этап 1
