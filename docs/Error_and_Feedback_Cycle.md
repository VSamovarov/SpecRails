---
version: 2.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-09
---

# 🔄 Цикл обработки ошибок и обратной связи

Как SpecRails превращает ошибки в улучшения через автоматическую и человеческую обратную связь.

---

## 🎯 Суть

Ошибки — это не проблема, а **сигнал для улучшения**. SpecRails работает по принципу:

1. **Ошибка обнаружена** → Validator фиксирует
2. **AI исправляет** → автоматически или с подсказкой
3. **Человек проверяет** → подтверждает или корректирует
4. **Система учится** → сохраняет паттерн для будущего

Этот цикл делает SpecRails **самообучающейся системой**.

---

## 🔍 Типы ошибок

### 1. Синтаксические (Syntax)

**Что это:** Некорректный формат YAML/JSON

**Пример:**
```yaml
# ❌ Ошибка
fields:
  - name email
    type: string
    
# ✅ Правильно
fields:
  - name: email
    type: string
```

**Кто находит:** Parser  
**Кто исправляет:** AI автоматически  
**Действие:** Переделать с правильным синтаксисом

---

### 2. Структурные (Structure)

**Что это:** Несоответствие JSON Schema

**Пример:**
```yaml
# ❌ Ошибка - неверный тип
field:
  name: birthDate
  type: text  # должно быть date
  
# ✅ Правильно
field:
  name: birthDate
  type: date
  min: "1900-01-01"
  max: "2025-12-31"
```

**Кто находит:** Schema Validator  
**Кто исправляет:** AI по отчёту  
**Действие:** Исправить тип и добавить обязательные атрибуты

---

### 3. Логические (Logic)

**Что это:** Некорректная комбинация атрибутов

**Пример:**
```yaml
# ❌ Ошибка - нет max для min
field:
  name: age
  type: number
  min: 18
  # отсутствует max
  
# ✅ Правильно
field:
  name: age
  type: number
  min: 18
  max: 120
```

**Кто находит:** Core Rules  
**Кто исправляет:** AI с подсказкой  
**Действие:** Добавить недостающие правила

---

### 4. Семантические (Semantic)

**Что это:** Поле не соответствует бизнес-логике

**Пример:**
> Аналитик: «Поле 'email' не должно быть обязательным для роли 'Guest'»

**Кто находит:** Analyst  
**Кто исправляет:** AI по обратной связи или вручную  
**Действие:** Скорректировать правила с учётом роли

---

## 🔄 Механизм обработки ошибок

### Шаг 1: Обнаружение

```
AI создаёт DSL
    ↓
Validator проверяет
    ↓
[Ошибка найдена]
```

---

### Шаг 2: Классификация

Validator определяет тип и серьёзность:

```json
{
  "valid": false,
  "errors": [
    {
      "field": "email",
      "issue": "отсутствует атрибут 'required'",
      "severity": "error",
      "hint": "добавьте required: true или false"
    }
  ]
}
```

**Уровни серьёзности:**
- `error` — блокирует использование DSL
- `warning` — можно использовать, но не рекомендуется
- `info` — подсказка для улучшения

---

### Шаг 3: Формирование обратной связи

**Feedback Manager** преобразует отчёт в понятный текст для AI:

```
Ты создал DSL с ошибками:

1. Поле 'email': отсутствует атрибут 'required'
   Подсказка: добавь required: true или false

2. Поле 'birthDate': неверный тип
   Должно быть: type: date
   Сейчас: type: text

Пожалуйста, исправь DSL согласно схеме spec.form.v1
```

---

### Шаг 4: Исправление AI

AI получает обратную связь и пересоздаёт DSL:

```yaml
# Исправленная версия
fields:
  - name: email
    type: string
    required: true  # ✅ добавлено
    
  - name: birthDate
    type: date      # ✅ исправлено
    min: "1900-01-01"
    max: "2025-12-31"
```

---

### Шаг 5: Повторная валидация

```
Validator проверяет снова
    ↓
[Ошибок нет] → передача в Preview
    ↓
[Ошибки остались] → повтор цикла с уточнением
```

---

## 👥 Человеческая обратная связь

### Зачем нужна

AI исправляет технические ошибки, но **смысловые** проверяет человек.

**Примеры:**
- Поле расположено неудобно
- Текст подсказки непонятен
- Логика не соответствует процессу

---

### Как работает

1. **Analyst видит preview**
2. **Находит проблему**
3. **Даёт обратную связь:**

> «Поле 'Дата рождения' должно быть над полем 'Email', а не под ним»

4. **AI обновляет DSL** с учётом комментария
5. **Validator проверяет**
6. **Preview показывает новый вариант**

---

### Типы человеческой обратной связи

| Тип               | Пример                                         | Как AI реагирует           |
|-------------------|------------------------------------------------|----------------------------|
| **Семантическая** | «Email не должен быть обязательным для гостей» | Обновляет правила required |
| **Визуальная**    | «Кнопка 'Отмена' не нужна»                     | Добавляет hidden: true     |
| **Контекстная**   | «Это поле только для роли Teacher»             | Обновляет ACL правила      |
| **UX**            | «Слишком длинная подсказка»                    | Сокращает description      |

---

## 📊 Формат хранения обратной связи

Каждая обратная связь сохраняется в **Feedback Registry**:

```yaml
feedback_entry:
  id: FBK-2025-11-09-001
  timestamp: "2025-11-09T10:00:00Z"
  
  source:
    type: analyst
    user: analyst_viktor
  
  context:
    contract: form.contract.v2
    ai_model: gpt-5
    file: form.student.v1.yaml
  
  issue:
    type: semantic
    field: email
    description: "Field should not be required for Guest role"
  
  action:
    suggested_fix: "Add role-based required logic"
    applied: true
    validated_by: senior_analyst
  
  result:
    validation_improved: true
    accuracy_gain: +8%
```

---

## 🔁 Цикл обучения

### Как система улучшается

1. **Сбор:** Ошибки и обратная связь собираются в Feedback Registry
2. **Анализ:** AI Learner находит паттерны (часто встречающиеся ошибки)
3. **Предложение:** Генерирует улучшения контрактов
4. **Валидация:** Человек проверяет предложения
5. **Применение:** Обновляются контракты и промпты

---

### Пример эволюции контракта

**Версия 1.0** (начальная)
```yaml
contract: form.contract.v1
fields:
  - type: email
    # нет правил required
```

**После обратной связи → v1.1**
```yaml
contract: form.contract.v1.1
fields:
  - type: email
    required: true  # добавлено на основе фидбэка
```

**После ещё одной → v1.2**
```yaml
contract: form.contract.v1.2
fields:
  - type: email
    required:
      default: true
      guest: false  # роль Guest - необязательно
```

---

## 📈 Метрики эффективности

| Метрика                       | Что показывает                                         |
|-------------------------------|--------------------------------------------------------|
| **Feedback Utilization Rate** | Процент обратной связи, которая привела к изменениям   |
| **Error Reduction Rate**      | Насколько уменьшилось количество ошибок со временем    |
| **Auto-fix Success Rate**     | Доля ошибок, исправленных AI без участия человека      |
| **Learning Gain Index**       | Улучшение точности валидации после применения фидбэков |
| **Time to Fix**               | Среднее время от обнаружения ошибки до исправления     |

---

## 🧠 Компоненты системы

### Feedback Collector
Собирает обратную связь из разных источников:
- Комментарии аналитиков в preview
- Отчёты Validator
- Ручные правки DSL
- Результаты ревью

---

### Feedback Manager
Классифицирует и преобразует обратную связь:
- Определяет тип ошибки
- Оценивает серьёзность
- Формирует инструкции для AI
- Создаёт структурированный отчёт

---

### AI Repair Loop
Автоматически исправляет DSL:
- Получает отчёт об ошибках
- Анализирует контракт
- Генерирует исправленную версию
- Отправляет на повторную валидацию

---

### Feedback Learner
Учится на истории:
- Анализирует паттерны ошибок
- Предлагает улучшения контрактов
- Генерирует обновления промптов
- Оценивает эффективность изменений

---

### Learning History Tracker
Хранит всю историю:
- Какие ошибки были
- Как исправлялись
- Что улучшилось
- Какие паттерны повторяются

---

## ✅ Преимущества системы

| Преимущество               | Результат                                        |
|----------------------------|--------------------------------------------------|
| **Self-Healing**           | AI учится исправлять ошибки без помощи           |
| **Transparency**           | Видна причина каждого исправления                |
| **Continuous Improvement** | Система становится лучше с каждым использованием |
| **Knowledge Retention**    | Ошибки не повторяются                            |
| **Human Control**          | Финальное решение всегда за человеком            |

---

## 🔒 Архитектурная диаграмма

```
┌─────────────────────────────────────────────────┐
│              Error Detection                    │
│  (Validator находит ошибку в DSL)               │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│          Feedback Classification                │
│  (Определяется тип и серьёзность)               │
└────────────────┬────────────────────────────────┘
                 │
         ┌───────┴────────┐
         │                │
         ▼                ▼
┌─────────────┐  ┌──────────────────┐
│  Auto-fix   │  │ Human Feedback   │
│   (AI Loop) │  │  (Analyst input) │
└──────┬──────┘  └────────┬─────────┘
       │                  │
       └────────┬─────────┘
                ▼
┌─────────────────────────────────────────────────┐
│           Corrected DSL                         │
│  (Исправленная версия)                          │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│        Re-validation                            │
│  (Проверка снова)                               │
└────────────────┬────────────────────────────────┘
                 │
         ┌───────┴────────┐
         │                │
    [✅ Pass]        [❌ Fail]
         │                │
         ▼                └──► (повтор цикла)
┌──────────────┐
│   Preview    │
└──────────────┘
         │
         ▼
┌──────────────────────────────────────────────────┐
│      Feedback Registry                           │
│  (История для обучения)                          │
└──────────────────────────────────────────────────┘
```

---

## 🧭 Архитектурный вывод

> **Ошибки — это не сбои, а обучающие сигналы.**

SpecRails превращает каждую ошибку в улучшение:

1. **Фиксирует** через Validator
2. **Классифицирует** через Feedback Manager
3. **Исправляет** через AI Repair Loop
4. **Проверяет** через человека
5. **Учится** через Feedback Learner
6. **Запоминает** через Learning History

Это делает систему **самосовершенствующейся** при сохранении контроля и прозрачности.

---

## 📚 Связанные документы

- [Глоссарий терминов](Terminology_Glossary.md) — определения Feedback, Validator, Contract
- [Validation Loop](Validation_Loop_Principle.md) — как валидация встроена в цикл
- [Observability Framework](Observability_Framework.md) — метрики обучения и эффективности
