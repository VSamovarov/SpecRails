---
version: 1.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-11
---

# 🏗️ SpecRails Reference Architecture

## 🎯 Цель документа
Описать высокоуровневую архитектуру SpecRails с чёткими границами ответственности модулей.

---

## 📐 Архитектурные принципы

### 1. Изоляция ядра (Core Isolation)
**Принцип:** Core Layer не зависит от Process и Governance.

**Граница:**
```
Core (Validator, Schema Registry, Prompt Registry)
  ↓ [PromptManifestDTO, ValidationResultDTO]
Process (Parser Agent, Orchestrator, Validation Loop)
  ↓ [Metrics, Events]
Governance (Policy Matrix, Observability)
```

### 2. Контрактное взаимодействие (Contract-Based Integration)
**Принцип:** Модули обмениваются данными только через формализованные DTO.

**Ключевые контракты:**
- `PromptManifestDTO` — Parser → Prompt Registry
- `ValidationState` — Orchestrator → Validation Loop
- `PolicyHook` — Observability → Governance

### 3. Односторонние зависимости (Unidirectional Dependencies)
**Принцип:** Зависимости направлены сверху вниз, обратная связь через события.

```
Core ← Process ← Governance
  ↑________________↓
      [Feedback Events]
```

---

## 🧩 Модульные границы

### Core Layer — Ядро системы

**Ответственность:**
- Формальная валидация структуры (Validator)
- Управление схемами (Schema Registry)
- Хранение промптов с версиями (Prompt Registry)

**Не отвечает за:**
- Создание или преобразование DSL
- Оркестрацию процессов
- Принятие решений о готовности DSL
- Сбор метрик

**Интерфейс выхода:** `ValidationResultDTO`

---

### Process Layer — Исполнение и координация

**Ответственность:**
- Генерация DSL из текста (Parser Agent)
- Управление жизненным циклом валидации (Validation Loop)
- Координация агентов (Orchestrator)
- UX-валидация через Preview
- Обработка обратной связи (Feedback Cycle)

**Не отвечает за:**
- Формальная валидация структуры DSL
- Управление политиками
- Долгосрочное хранение

**Интерфейс входа:** текст от Analyst  
**Интерфейс выхода:** `PromptManifestDTO`, `ValidationState`, события

---

### Governance Layer — Контроль и наблюдение

**Ответственность:**
- Определение политик качества (Policy Matrix)
- Сбор и анализ метрик (Observability)
- Автоматическая реакция на отклонения (Policy Hook)
- Аудит изменений

**Не отвечает за:**
- Выполнение валидации
- Создание DSL
- Прямое управление агентами

**Интерфейс входа:** события, метрики  
**Интерфейс выхода:** `PolicyHook` триггеры

---

## 🔄 Потоки данных

### 1. Создание промпта
```
Analyst (текст) 
  → Parser Agent (генерация DSL)
  → PromptManifestDTO
  → Validator (проверка структуры)
  → ValidationResultDTO
  → Prompt Registry (регистрация)
  → ValidationState: draft
```

### 2. Цикл валидации
```
Orchestrator: ValidationState = generating
  → Validator (проверка структуры)
  → ValidationState = validating
  → Preview (UX-рендер)
  → ValidationState = previewing
  → Analyst (смысловая оценка)
  → ValidationState = reviewing
  → [approved | rejected]
```

### 3. Контур обратной связи
```
Observability (метрики)
  → Policy Hook (превышение порога)
  → Governance (триггер действия)
  → Validation Loop (применение ограничений)
  → Observability (новые метрики)
```

---

## 🔐 Принципы безопасности

### Sandbox Context
**Что изолируется:**
- Модули не имеют прямого доступа к файловой системе
- Нет возможности выполнять произвольный код
- Ограничен набор API (только PromptAPI, ValidatorAPI, ContextAPI read-only)

### Контроль версий
**Что версионируется:**
- JSON Schema контрактов
- PromptManifestDTO
- Policy Matrix правила

---

## ✅ Критерии архитектурной корректности

| Критерий | Проверка |
|----------|----------|
| Изоляция ядра | Core не импортирует Process/Governance модули |
| Контрактное взаимодействие | Все передачи данных через DTO со схемами |
| Односторонние зависимости | Обратная связь только через события |
| Операционализация процессов | Каждый процесс имеет state machine |
| Замкнутость контуров | Observability влияет на Governance через Policy Hook |

---

## 📚 Связанные документы

- `Terminology_Glossary.md` — определения всех терминов
- `Core_Principle.md` — детализация Core Layer
- `Validation_Loop_Principle.md` — детализация Process Layer
- `Governance_Policy_Matrix.md` — детализация Governance Layer
- `PromptManifestDTO.json` — схема основного контракта

---

**Статус:** Draft  
**Следующий шаг:** Утверждение архитектурным советом  
**Обновление:** После детализации каждого модуля
