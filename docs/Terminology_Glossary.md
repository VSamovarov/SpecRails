---
version: 2.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-08
---

# 📖 Глоссарий терминов SpecRails

Единый справочник ключевых понятий и терминов, используемых в документации и архитектуре SpecRails.

---

## 🎯 Core — Ядро системы

### DSL (Domain-Specific Language)
**Что это:** Структурированное описание спецификации в формате YAML/JSON, которое понимают и люди, и машины.

**Примеры:** `.form.yaml`, `.model.yaml`, `.acl.yaml`

**Зачем:** DSL — источник истины. Из него генерируются формы, документация, код валидации.

---

### Validator (Валидатор)
**Что это:** Компонент ядра, который проверяет корректность DSL по схеме и правилам.

**Что проверяет:**
- Синтаксис (валидный YAML/JSON)
- Структуру (соответствие JSON Schema)
- Типы полей и обязательные атрибуты
- Логические ограничения (например, `min < max`)

**Важно:** Валидатор НЕ исправляет ошибки автоматически. Он только находит и сообщает о них.

---

### Parser (Парсер)
**Что это:** Модуль, который преобразует текстовое описание (user story, требование) в структурированный DSL.

**Как работает:** Получает текст от AI или аналитика → разбирает на элементы → создаёт YAML/JSON структуру.

---

### Contract (Контракт)
**Что это:** Чёткое описание того, что AI должен вернуть в ответ на запрос.

**Включает:**
- Формат ответа (JSON Schema)
- Обязательные поля
- Допустимые значения
- Примеры корректных ответов

**Зачем:** Контракт гарантирует, что AI вернёт предсказуемый результат, который можно валидировать.

---

### Schema Registry (Реестр схем)
**Что это:** Хранилище JSON Schema для разных версий DSL.

**Зачем:** Позволяет управлять версиями спецификаций и обеспечивает обратную совместимость.

---

## 🔄 Runtime — Исполнение и валидация

### Validation Loop (Цикл валидации)
**Что это:** Итеративный процесс проверки и улучшения DSL.

**Участники:**
1. **AI Agent** — создаёт черновой DSL из текста
2. **Validator** — проверяет формальную корректность
3. **Analyst** — проверяет смысл и логику через preview

**Как работает:** AI → Validator → Preview → Analyst → обратная связь → AI (повтор до готовности).

---

### Preview (Превью)
**Что это:** Визуальное отображение DSL в виде формы, страницы или документа.

**Зачем:** Позволяет аналитику проверить, что спецификация соответствует ожиданиям, не читая YAML.

---

### Feedback (Обратная связь)
**Что это:** Информация об ошибках или улучшениях, которую получает AI или аналитик.

**Типы:**
- **Machine Feedback** — отчёт валидатора с конкретными ошибками
- **Human Feedback** — комментарии аналитика о том, что нужно изменить

---

### Context (Контекст)
**Что это:** Набор данных, доступных AI во время генерации DSL.

**Включает:**
- История предыдущих запросов
- Конфигурация проекта
- Примеры существующих спецификаций
- Ограничения и политики

**Важно:** Контекст в SpecRails **неизменяемый** (immutable) — модули не могут его изменять напрямую.

---

### Sandbox (Песочница)
**Что это:** Изолированная среда для выполнения AI-запросов и модулей.

**Зачем:** Защищает систему от вредоносного кода и неконтролируемого доступа к данным.

**Ограничения:**
- Нет доступа к файловой системе
- Нет доступа к сети
- Лимиты по памяти и процессору

---

## 📋 Prompt Management — Управление промптами

### Prompt (Промпт)
**Что это:** Инструкция для AI, описывающая что нужно сделать.

**Структура:**
- Цель задачи
- Входные данные
- Контракт (формат ответа)
- Примеры и ограничения

---

### Prompt Registry (Реестр промптов)
**Что это:** Централизованное хранилище всех промптов с версиями и метаданными.

**Содержит:**
- Название и версию промпта
- Контракт ответа
- Зависимости от других промптов
- Статус (active, deprecated, experimental)

---

### Runtime Manifest (Манифест выполнения)
**Что это:** Описание модуля или утилиты для SpecRails.

**Включает:**
- Имя и версию модуля
- Зависимости
- API, к которым модуль имеет доступ
- Ограничения безопасности

**Зачем:** Позволяет ядру контролировать, какие модули загружены и что они могут делать.

---

## 🛡️ Governance — Управление и контроль

### Governance (Управление)
**Что это:** Набор правил и политик, которые контролируют работу системы.

**Включает:**
- Кто может изменять контракты
- Когда требуется ревью
- Какие метрики отслеживаются
- Как обрабатываются изменения

---

### Drift (Дрейф)
**Что это:** Постепенное отклонение AI-ответов от ожидаемого поведения.

**Пример:** AI начинает генерировать DSL с незначительными изменениями структуры, которые накапливаются со временем.

**Как контролируем:** Измеряем Drift Index и требуем подтверждения при превышении порога.

---

### Audit Trail (Аудит-лог)
**Что это:** Полная история изменений в системе.

**Записывает:**
- Кто и когда изменил контракт или DSL
- Какие версии были использованы
- Результаты валидации
- Решения по ревью

---

## 📊 Observability — Наблюдаемость

### Telemetry (Телеметрия)
**Что это:** Автоматический сбор данных о работе системы.

**Что собираем:**
- Время выполнения AI-запросов
- Количество ошибок валидации
- Частота использования промптов
- Изменения в контрактах

---

### Observability (Наблюдаемость)
**Что это:** Способность понимать состояние системы через метрики, логи и трейсы.

**Включает:**
- **Metrics** — числовые показатели (время ответа, ошибки)
- **Logs** — текстовые записи о событиях
- **Traces** — путь выполнения запроса через компоненты

---

### Drift Index (Индекс дрейфа)
**Что это:** Числовое значение, показывающее степень отклонения AI от эталона.

**Как считается:** Сравнивается новая версия DSL с предыдущей, вычисляется процент изменений.

**Пороги:**
- `< 0.05` — стабильно
- `0.05-0.15` — требуется внимание
- `> 0.15` — требуется ревью

---

## 🔧 Extensibility — Расширяемость

### Module (Модуль)
**Что это:** Независимый компонент, который добавляет функциональность в SpecRails.

**Типы:**
- **Utility** — вспомогательные функции
- **Generator** — создание новых типов DSL
- **Validator** — дополнительные правила проверки
- **Renderer** — визуализация специфичных форматов

---

### SDK (Software Development Kit)
**Что это:** Набор инструментов для создания модулей и расширений.

**Предоставляет:**
- API для работы с ядром
- Шаблоны для создания модулей
- Утилиты для тестирования
- Документацию

---

### Extension API (API расширений)
**Что это:** Интерфейс, через который модули взаимодействуют с ядром.

**Основные API:**
- **PromptAPI** — выполнение AI-запросов
- **ValidatorAPI** — проверка DSL
- **ContextAPI** — доступ к контексту (только чтение)
- **RegistryAPI** — регистрация модулей

---

## 🔁 Workflow — Рабочие процессы

### CI/CD Integration (Интеграция с CI/CD)
**Что это:** Автоматическая проверка спецификаций в процессе разработки.

**Что проверяем:**
- Валидность всех DSL в репозитории
- Соответствие контрактам
- Отсутствие критического дрейфа
- Полноту документации

---

### Analyst (Аналитик)
**Кто это:** Человек, который создаёт и проверяет спецификации.

**Роль в цикле:**
- Даёт текстовое описание задачи
- Проверяет preview
- Корректирует AI через обратную связь
- Утверждает финальную версию DSL

---

### AI Agent (AI-агент)
**Что это:** Языковая модель (LLM), которая генерирует DSL из текста.

**Роль:** Преобразует естественный язык в структурированные данные, но не является источником истины.

**Ограничения:** Работает только через контракты, не имеет прямого доступа к данным.

---

## 📚 Связь терминов

```
Analyst → AI Agent → DSL → Validator → Preview → Feedback → (цикл повторяется)
              ↓
          Contract → Schema Registry
              ↓
          Context → Sandbox → Runtime Manifest
              ↓
          Telemetry → Drift Index → Governance
```

---

## ✅ Принципы использования терминов

1. **DSL** — всегда источник истины, не "конфиг" или "спека"
2. **Validator** — проверяет, но не исправляет
3. **Contract** — определяет формат, не "промпт-шаблон"
4. **Context** — неизменяемый, не "параметры"
5. **Feedback** — конкретная информация, не "комментарий"

---

Этот глоссарий — живой документ. При добавлении новых концепций обновляйте его в первую очередь.

## Dependencies
- Terminology_Glossary.md


## Related Documents
- DOCUMENTATION_MAP.md


## Key Concepts
- To be expanded.
