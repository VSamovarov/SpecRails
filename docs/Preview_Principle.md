---
version: 2.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-09
---

# 👁️ Принцип Preview в SpecRails

Как визуализация помогает проверять качество спецификаций.

---

## 🎯 Что такое Preview

**Preview** — это визуальное представление DSL, которое показывает аналитику **как будет выглядеть** форма, страница или модель.

**Важно:** Preview — это **НЕ часть ядра**, а инструмент для человеческой валидации.

---

## 🧩 Зачем нужен Preview

### Проблема

Validator проверяет **техническую** корректность:
- ✅ Синтаксис правильный
- ✅ Структура соответствует схеме
- ✅ Типы полей корректны

Но Validator **не понимает смысл**:
- ❓ Удобно ли расположены поля?
- ❓ Понятен ли текст подсказок?
- ❓ Соответствует ли логика бизнес-процессу?

### Решение

Preview показывает **как это выглядит**, чтобы аналитик мог проверить смысл.

---

## 🔄 Место Preview в цикле

```
AI создаёт DSL
    ↓
Validator проверяет структуру ✅
    ↓
Preview показывает визуально 👁️
    ↓
Analyst проверяет смысл
    ↓
[OK] → Утверждает
[Нужны правки] → даёт обратную связь AI
```

Preview — это **глаза** системы: не принимает решений, но даёт возможность увидеть результат.

---

## 🧱 Функции Preview

### 1. Визуализация DSL

**Что показывает:**
- Формы с полями
- Страницы с разделами
- Таблицы ACL
- Диаграммы моделей

**Пример:**

DSL:
```yaml
form:
  name: student_registration
  fields:
    - name: fullName
      type: string
      required: true
      label: "Полное имя"
    - name: email
      type: email
      required: true
      label: "Email"
```

Preview:
```
┌─────────────────────────────┐
│  Регистрация студента       │
├─────────────────────────────┤
│ Полное имя *                │
│ [_____________________]     │
│                             │
│ Email *                     │
│ [_____________________]     │
│                             │
│ [Отправить] [Отмена]        │
└─────────────────────────────┘
```

---

### 2. Интерактивная корректировка

Аналитик может дать обратную связь прямо в preview:

> «Поле 'Email' должно быть над полем 'Полное имя'»

AI обновляет DSL, Validator проверяет, Preview показывает новый вариант.

---

### 3. Проверка UX

Preview помогает увидеть:
- Слишком длинные подсказки
- Неудобное расположение кнопок
- Непонятные названия полей
- Лишние элементы

**Пример:**

> Аналитик: «Кнопка 'Отмена' не нужна, это форма регистрации»

AI убирает кнопку из DSL.

---

## ⚙️ Как работает Preview

### Процесс

```
1. Core валидирует DSL → [✅ Valid]
2. Core передаёт DSL в Preview Renderer
3. Renderer создаёт HTML/UI представление
4. Аналитик видит результат в браузере/VS Code
```

### Важно

Preview получает **только валидированный DSL**. Если есть ошибки — Preview не показывается, сначала нужно исправить.

---

## 🔐 Взаимодействие с ядром

| Направление | Данные | Формат | Примечание |
|-------------|--------|--------|------------|
| **Core → Preview** | Валидированный DSL | JSON/YAML | Только read-only |
| **Preview → AI** | Обратная связь | Текст | Комментарии аналитика |
| **Preview → Core** | Изменения | Не напрямую | Через AI Agent |

Preview **не может изменить DSL напрямую**. Все правки идут через AI или ручное редактирование → Validator → Preview.

---

## 🧠 Принципы работы Preview

### 1. Preview — это слой восприятия

**Не делает:**
- ❌ Не проверяет корректность DSL
- ❌ Не изменяет структуру
- ❌ Не принимает решений

**Делает:**
- ✅ Показывает результат
- ✅ Собирает обратную связь
- ✅ Помогает понять смысл

---

### 2. Минимальная зависимость от технологий

Preview может использовать:
- Простой HTML + CSS
- Vue/React компоненты
- Встроенный рендерер VS Code
- Любой другой UI фреймворк

**Главное:** Preview работает через **публичный API ядра** и не зависит от внутренней реализации.

---

### 3. Независимость от AI

Preview может работать **без AI**:
- Аналитик пишет DSL вручную
- Validator проверяет
- Preview показывает результат

AI ускоряет процесс, но **не обязателен** для preview.

---

## ✅ Примеры использования

### Пример 1: Проверка формы

**DSL:**
```yaml
form:
  name: teacher_profile
  fields:
    - name: subject
      type: string
      label: "Предмет"
    - name: experience
      type: number
      label: "Стаж (лет)"
      min: 0
      max: 50
```

**Preview:**
```
┌─────────────────────────────┐
│  Профиль преподавателя      │
├─────────────────────────────┤
│ Предмет                     │
│ [_____________________]     │
│                             │
│ Стаж (лет)                  │
│ [___] (от 0 до 50)          │
│                             │
│ [Сохранить]                 │
└─────────────────────────────┘
```

**Обратная связь аналитика:**
> «Добавь подсказку для поля 'Предмет' — какие предметы можно указывать»

---

### Пример 2: Проверка ACL

**DSL:**
```yaml
acl:
  role: student
  permissions:
    - view: own_profile
    - edit: own_profile
    - view: courses
```

**Preview (таблица):**
```
Роль: Студент
┌──────────────┬───────────────┐
│ Действие     │ Объект        │
├──────────────┼───────────────┤
│ Просмотр     │ Свой профиль  │
│ Изменение    │ Свой профиль  │
│ Просмотр     │ Курсы         │
└──────────────┴───────────────┘
```

**Обратная связь:**
> «Студент не должен иметь права изменять свой профиль, только администратор»

---

## 📊 Архитектурная диаграмма

```
┌────────────────────────────────────────┐
│          Analyst (Человек)             │
└──────────┬─────────────────▲───────────┘
           │                 │
    (запрос)           (визуальный
           │            результат)
           ▼                 │
┌────────────────────────────┼───────────┐
│         AI Agent           │           │
└──────────┬─────────────────┘           │
           │                             │
    (генерирует DSL)                     │
           │                             │
           ▼                             │
┌──────────────────────────────────────┐ │
│         Core Engine                  │ │
│  ┌────────┐      ┌────────────┐     │ │
│  │ Parser │─────>│ Validator  │     │ │
│  └────────┘      └─────┬──────┘     │ │
│                        │             │ │
│                   [✅ Valid]         │ │
└────────────────────────┼─────────────┘ │
                         │               │
                         ▼               │
                  ┌─────────────┐        │
                  │   Preview   │────────┘
                  │  Renderer   │
                  └─────────────┘
```

---

## 🔒 Ограничения Preview

### Что НЕ может Preview

1. **Исправлять ошибки**  
   Если DSL невалиден — Preview не показывается

2. **Менять DSL напрямую**  
   Все изменения через AI или ручное редактирование

3. **Заменить Validator**  
   Preview показывает только валидный DSL

4. **Работать без ядра**  
   Preview зависит от Core API

---

## ✅ Преимущества Preview

| Преимущество | Результат |
|--------------|-----------|
| **Понятность** | Аналитик видит результат без чтения YAML |
| **Быстрая обратная связь** | Можно сразу указать на проблемы |
| **UX-проверка** | Видны проблемы удобства использования |
| **Контроль смысла** | Гарантия, что DSL соответствует задаче |

---

## 🧭 Стратегический вывод

> **Preview — это мост между машиной и человеком.**

Preview превращает технический DSL в **понятное представление**, которое аналитик может проверить.

Без Preview:
- ❌ Нужно читать YAML вручную
- ❌ Сложно увидеть UX-проблемы
- ❌ Обратная связь менее конкретна

С Preview:
- ✅ Визуальная проверка за секунды
- ✅ Интуитивная обратная связь
- ✅ Контроль качества UX

**SpecRails = Validator (машина) + Preview (человек) = Качественные спецификации**

---

## 📚 Связанные документы

- [Глоссарий терминов](Terminology_Glossary.md) — определения Preview, Renderer
- [Validation Loop](Validation_Loop_Principle.md) — место Preview в цикле
- [Core Principle](Core_Principle.md) — почему Preview не часть ядра
