version: 2.1
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-09
 
# 🧩 Prompt Registry и Runtime Manifest — принципиальный обзор

Этот документ описывает архитектурные принципы управления промптами и активными модулями в SpecRails. Сохранены примеры YAML, жизненный цикл и диаграммы как опорные артефакты. Реализационный код (классы, SDK вызовы) удалён, чтобы подчеркнуть концептуальный слой.

---

## 🎯 Назначение

Система должна знать: (1) какой контракт / промпт применим к задаче; (2) какие модули и утилиты активны сейчас. Для этого существуют два взаимодополняющих артефакта:
– Prompt Registry: источник истины о промптах, их версиях, статусах и правилах извлечения DSL.
– Runtime Manifest: снимок загруженных модулей, их параметров, контрольной целостности и связей с контрактами.

Основные целевые свойства: воспроизводимость, управляемая эволюция, безопасность исполнения и трассируемость.

---

## 📋 Prompt Registry

Централизованный конфигурируемый реестр промптов. Parser обращается к нему за метаданными извлечения. Реестр не содержит исполняемую логику — только декларативные параметры.

Состав записи:
– ID и версия (семантическая схема).
– Контракт (описание ожидаемой структуры; ассоциированная схема для валидации в ядре).
– Формат и правила извлечения (YAML / JSON, маркеры, fallbacks).
– Зависимости между контрактами (граф композиции).
– Статус жизненного цикла (active / deprecated / archived).
– История изменений (аудит).

### Пример записи

```yaml
registry:
  - id: form.contract.v2
    status: active
    contract_file: "contracts/form.contract.yaml"
    schema: "schemas/form.dsl.schema.json"
    
    # Информация для Parser
    format: yaml           # Parser ищет YAML блоки
    extraction_rules:
      marker_start: "```yaml"
      marker_end: "```"
      fallback: strip_all_markdown
    
    depends_on:
      - base.contract.v1
      - model.contract.v1
    created_at: "2025-10-15"
    owner: "specrails-core"

  - id: model.contract.v1
    status: deprecated
    replaced_by: model.contract.v2
    deprecated_since: "2025-11-01"
```

---

### Использование реестра Parser'ом
Parser — единственный компонент, запрашивающий контракт по идентификатору и извлекающий параметры генерации и правила структурирования. Процесс концептуально включает: выбор контракта → формирование промпта → вызов AI → извлечение DSL по правилам → возврат результата или ошибки. Низкоуровневый код не приводится — важна последовательность шагов.

---

### Жизненный цикл промпта

Стадии контролируют эволюцию и допустимость использования:

```
Draft → Review → Published → Active → Deprecated → Archived
```

| Стадия         | Что происходит                          |
|----------------|-----------------------------------------|
| **Draft**      | Промпт создан, но ещё не проверен       |
| **Review**     | Проходит ревью архитектором или QA      |
| **Published**  | Добавлен в реестр, получил версию       |
| **Active**     | Используется для генерации DSL          |
| **Deprecated** | Устарел, но ещё поддерживается          |
| **Archived**   | Снят с эксплуатации, хранится в истории |

---

### Версионирование
Следуется Semantic Versioning (major/minor/patch). Правила применяются к контракту как к спецификации ожиданий модели. Rollback доступен за счёт хранения всех активных и снятых версий.

---

## Dependencies
Граф зависимостей проверяется перед использованием: отсутствующие или несовместимые версии блокируют применение контракта до приведения окружения к согласованному состоянию.

---

## ⚙️ Runtime Manifest

Описание активного набора модулей и их параметров выполнения. Служит для воспроизводимости и аудита: «какие элементы были загружены в момент генерации».

### Пример манифеста

```yaml
runtime_manifest:
  version: 1.2.0
  build_date: "2025-11-08"
  environment: vscode-plugin
  
  modules:
    - name: core.runtime
      version: 1.0.0
      status: active
    
    - name: form.builder
      version: 0.4.2
      type: utility
      contract: form.contract.v2
      enabled: true
    
    - name: model.designer
      version: 0.2.1
      type: utility
      enabled: false  # отключен
  
  contracts:
    - id: form.contract.v2
      schema: schemas/form.dsl.schema.json
      checksum: 8a2f9bcd...
```

---

### Типы модулей

| Тип       | Назначение                  | Пример                       |
|-----------|-----------------------------|------------------------------|
| Core      | Базовые механизмы ядра      | core.runtime, validator      |
| Utility   | Вспомогательные операции    | form.builder, model.designer |
| Extension | Расширение функциональности | analytics, export.pdf        |
| Renderer  | Визуализация DSL / форматов | preview.html, preview.vue    |

---

### Регистрация
Концептуально модуль объявляет имя, тип, версии и зависимости; после проверки совместимости и целостности он становится доступен. Низкоуровневые SDK вызовы не приводятся.

---

### Проверка зависимостей
Несоответствие версии или отсутствие требуемого модуля блокирует загрузку до исправления конфигурации.

---

## 🔐 Безопасность: Sandbox & Integrity

### Sandbox Policy
Изоляция выполнения: разрешённые публичные API, запрещённый прямой доступ к ресурсам и лимиты по времени/памяти.

```yaml
sandbox_policy:
  allowed_apis:
    - PromptAPI
    - ValidatorAPI
    - ContextAPI
  restricted:
    - fs
    - http
    - child_process
  limits:
    timeout_ms: 5000
    memory_mb: 128
```

Нарушение правил переводит модуль в состояние quarantine.

---

### Integrity Verification
Контрольные суммы (SHA256) фиксируют неизменность артефактов.

```yaml
integrity_check:
  module: form.builder
  version: 0.4.2
  checksum: ffae1c27d...
  verified: true
  timestamp: "2025-11-08T10:00:00Z"
```

Несовпадение блокирует модуль до ручного подтверждения.

---

## 🔄 Совместная работа

### Загрузка окружения
Конвейер инициализации отражает последовательность: создание реестра → чтение манифеста → проверки зависимостей и целостности → загрузка базовых модулей → подготовка компонентов генерации (AI Agent, Parser) → запуск Orchestrator.

```
1. Application Layer стартует
2. Инициализирует Prompt Registry
3. Читает Runtime Manifest
4. Проверяет зависимости модулей
5. Проверяет checksums
6. Загружает Core
7. Создаёт AI Agent
8. Инициализирует Parser
9. Формирует Orchestrator
10. Система готова
```

---

### Выполнение промпта (вариант последовательности)
```
1. Аналитик формулирует задачу
2. Orchestrator определяет contractId
3. Сбор контекста
4. Вызов Parser (userInput + contractId)
5. Запрос контракта в Registry
6. Формирование промпта
7. Вызов AI Agent
8. Получение rawText
9. Извлечение DSL по правилам
10. Возврат DSL в Orchestrator
11. Валидация в ядре
12. Успех → Preview / Ошибка → Error Recovery
```

---

### Диаграмма взаимодействия (упрощённо)

```
┌──────────────────────────────────────────────┐
│         APPLICATION LAYER                    │
│                                              │
│  Создаёт: Registry, Parser, Orchestrator     │
└──────────────┬───────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────┐
│           ORCHESTRATOR                       │
│                                              │
│  processTask(input, contractId)              │
│         │                                    │
│         └──> Parser.parse(                   │
│                input,                        │
│                contractId                    │
│              )                               │
└──────────────┬───────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────┐
│              PARSER                          │
│      (Инкапсулирует AI)                      │
│                                              │
│  1. Registry.getContract(contractId)         │
│  2. Формирует промпт из template             │
│  3. AI Agent.execute(prompt) → rawText       │
│  4. Извлекает format и extraction_rules      │
│  5. Парсит rawText → DSL                     │
│  6. Возвращает { dsl, errors }               │
└──────────────┬───────────────────────────────┘
               │
               ├─────> Prompt Registry (промпты, правила)
               ├─────> AI Agent (вызов GPT/Claude)
               │
               ▼
┌──────────────────────────────────────────────┐
│            VALIDATOR (Core)                  │
│                                              │
│  validate(dsl, schemaId)                     │
└──────────────────────────────────────────────┘
```

---

### Выполнение промпта через модуль (альтернативный ракурс)
```
1. Задача → выбор контракта
2. Проверка доступности версии
3. Проверка зависимостей
4. Активный модуль вызывает PromptAPI
5. Генерация / возврат DSL
6. Валидация
7. Визуализация / обработка ошибки
```

---

## 📊 Телеметрия

Базовые метрики:

| Метрика              | Назначение                  |
|----------------------|-----------------------------|
| Active Modules       | Количество активных модулей |
| Sandbox Violations   | Нарушения политики изоляции |
| Dependency Conflicts | Несовместимые версии        |
| Load Latency         | Задержка загрузки модулей   |
| Integrity Failures   | Ошибки целостности          |

Пример snapshot:

```yaml
registry_snapshot:
  timestamp: "2025-11-08T10:15:00Z"
  active_modules: 
    - core.runtime
    - form.builder
  quarantined: []
  disabled: 
    - model.designer
  integrity_status: ok
  dependency_health: stable
```

---

## 🔁 Аудит и трассировка

Каждое использование контракта фиксируется для воспроизводимости:

```yaml
audit_entry:
  timestamp: "2025-11-08T10:20:00Z"
  contract_used: form.contract.v2
  ai_model: gpt-5
  input_hash: a7f3bc...
  result_hash: 8ffbc1a...
  validation: passed
  analyst: user@example.com
```

Цели аудита:
– Связать конкретный DSL с источником (контракт + модель).
– Повторить генерацию при тех же параметрах.
– Анализировать ошибки и отклонения ретроспективно.

---

## ✅ Ключевые преимущества

| Свойство          | Значение                                     |
|-------------------|----------------------------------------------|
| Воспроизводимость | Фиксированные правила извлечения на контракт |
| Безопасность      | Только проверенные модули и версии           |
| Прозрачность      | Явные параметры формата и извлечения         |
| Управляемость     | Централизованное обновление правил           |
| Трассируемость    | Полный путь от запроса к результату          |
| Изоляция          | Parser опирается только на Registry          |

---

## 🧭 Архитектурный вывод

Prompt Registry — источник истины для параметров генерации и извлечения. Runtime Manifest — актуальная модель загруженного окружения. Parser остаётся пассивным потребителем правил; управление версиями, безопасностью и зависимостями вынесено в реестр и манифест.

Результат: использование AI превращается из хаотичной активности в управляемый контролируемый процесс с воспроизводимостью, аудитом и возможностью эволюции без потери целостности.

---

## Related Documents
- [Core_Principle.md](Core_Principle.md)
- [Parser_Architecture.md](Parser_Architecture.md)
- [Governance_Policy_Matrix.md](Governance_Policy_Matrix.md)
- [Observability_Framework.md](Observability_Framework.md)
- DOCUMENTATION_MAP.md


## Key Concepts
- To be expanded.
