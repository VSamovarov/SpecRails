version: 2.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-08
 
# 🔁 Validation Loop — принцип

Документ фиксирует архитектурный принцип двухконтурной проверки DSL: формальная (машинная) и смысловая (человеческая). Реализационные детали намеренно опущены; оставлены концепции, роли, инварианты и поток.

---

## 🎯 Суть

Два слоя проверки качества DSL:
1.  **Машинная:** формальная структура, схема, типы, композиция. Результатом является готовый к прочтению DSL.
2.  **Смысловая (человеческая):** логика, порядок, понятность. Проводится аналитиком на основе либо сырого DSL, либо его визуального представления (`Preview`).

## 🔄 Цикл валидации

Цикл состоит из основного потока генерации и опционального шага визуализации.

**Основной поток:**
1.  **AI → Validator:** Система генерирует и формально валидирует DSL.
2.  **→ Analyst:** Аналитик получает на выходе корректный, готовый к прочтению DSL (в формате YAML/JSON).

На этом этапе аналитик уже может принять решение или дать обратную связь.

**Опциональный шаг: Предпросмотр (`Preview`)**
*   Если аналитику недостаточно текстового DSL, он может **по запросу** запросить его визуальное представление.
*   Система (через `Preview Service`) генерирует HTML-представление и показывает его аналитику.

**Завершение цикла:**
*   Основываясь на текстовом DSL и/или его `Preview`, аналитик дает обратную связь (`Feedback`) или утверждает результат.

---

## ⚙️ Машинная валидация

### Область проверки
– Синтаксис (валидный YAML/JSON).
– Структура (соответствие схеме / контракту).
– Типы и атрибуты (семантические требования типов).
– Композиция (include / component / reuse).
– Нормализация (метаданные, устранение неоднозначностей).

### Механизм (концептуально)
1. Генерация чернового DSL.
2. Формальная проверка (Validator, Core).
3. Отчёт об ошибках / отклонениях.
4. Коррекция генерации.
5. Повтор до прохождения критериев.

### Пример отчёта

```json
{
  "valid": false,
  "errors": [
    {
      "field": "email",
      "issue": "отсутствует обязательный атрибут 'required'",
      "severity": "error"
    },
    {
      "field": "birthDate",
      "issue": "неверный тип, должен быть 'date'",
      "severity": "error"
    }
  ]
}
```

Отчёт служит машинно‑читаемым feedback для очередной генерации.

---

## 👁️ Человеческая валидация

### Цель
Поймать смысловые несоответствия: порядок, удобство, термины, бизнес‑правила, контекст использования.

### Процесс (высокоуровнево)
1. Формально валидный DSL направляется в Preview.
2. Аналитик просматривает визуализацию / структуру.
3. Формирует корректировки (естественный язык / комментарии).
4. Генерация обновлённого DSL.
5. Возврат к машинной проверке.

### Пример обратной связи
> Поле "Дата рождения" сделать обязательным и переместить перед "Email".

---

## 🔄 Цикл в клиент-серверной архитектуре

Цикл валидации распределен между клиентом и сервером:

1.  **Клиент (Analyst):** Аналитик пишет текстовый запрос в UI (например, VS Code) и отправляет его на сервер.
2.  **Сервер (AI → Validator):** Сервер вызывает `AI Agent` для генерации DSL, а затем `Validator` для его машинной проверки.
3.  **Сервер → Клиент (Возврат DSL):** Сервер возвращает клиенту ответ, содержащий валидированный DSL (например, в формате YAML). Клиент отображает этот DSL аналитику.
4.  **Клиент (Ревью аналитика):** Аналитик изучает текстовый DSL. На этом этапе он уже может дать обратную связь или утвердить результат.
5.  **Опциональный запрос Preview:** Если аналитику требуется визуализация, клиент отправляет **дополнительный запрос** на сервер (например, на эндпоинт `/api/preview`), передавая ему полученный ранее DSL.
6.  **Сервер → Клиент (Возврат Preview):** Сервер генерирует HTML-представление `Preview` и возвращает его клиенту, который отображает его аналитику.
7.  **Клиент → Сервер (Feedback):** Основываясь на DSL и/или `Preview`, аналитик предоставляет обратную связь, которая отправляется на сервер для следующей итерации.

---

## 🧠 Роли

### AI Agent
Роль: генерация чернового DSL из текстового ввода. Ограничения: вариативность, требует формальной проверки, опирается на контракты.

---

### Validator (Core)
Роль: формальная верификация, генерация отчётов. Не интерпретирует бизнес‑смысл.

---

### Analyst
Роль: смысловая проверка, UX и бизнес‑логика, финальное утверждение.

---

## 📋 Принципы

| Принцип             | Смысл                                       |
|---------------------|---------------------------------------------|
| AI создаёт          | Генерация, не источник истины               |
| Validator проверяет | Формальная гарантия структуры               |
| Analyst утверждает  | Смысл и бизнес‑соответствие                 |
| Итеративность       | Накопительное улучшение, не одноразовый акт |
| Прозрачность        | Шаги и отчёты воспроизводимы                |

---

## 🔁 Ошибки

### Классы ошибок
| Тип              | Источник обнаружения   | Коррекция                                    |
|------------------|------------------------|----------------------------------------------|
| Синтаксис        | Parser/Validator       | Автоматическая генерация повторно            |
| Структура        | Validator              | Повторная генерация по отчёту                |
| Логика атрибутов | Validator              | Генерация + уточнение контракта              |
| Смысл            | Analyst                | Естественная корректировка → новая генерация |

---

### Исправление (упрощённо)
1. Обнаружение → 2. Отчёт → 3. Генерация повторно → 4. Ревалидция → 5. Ветвление (успех / новый цикл).

---

### Диалог (пример)
Validator сообщает: поле email отсутствует required. Feedback: «Добавь required: true к email». Генерация повторяется.

---

## ✅ Преимущества

| Свойство          | Эффект                                  |
|-------------------|-----------------------------------------|
| Надёжность        | Структурные ошибки не проникают в поток |
| Прозрачность      | Отчёты и визуализация понятны           |
| Качество          | Ранняя фильтрация дефектов              |
| Обучаемость       | История ошибок оптимизирует промпты     |
| Воспроизводимость | Контракт + вход дают ожидаемый DSL      |

---

## 🧭 Эффекты

### AI
Правила, структурированный feedback, улучшение генерации.

### Analyst
Контроль, визуальная проверка, лаконичные корректировки.

### Система
Стабильность, защита качества, накопление знаний.

---

## 🔒 Архитектурный вывод

Качество спецификации достигается итерацией, а не попыткой «с первого раза». Генерация вариативна, проверка детерминирована, утверждение смысловое. Их сочетание формирует управляемый контур повышения качества.

---

## Dependencies
- Terminology_Glossary.md


## Related Documents
- [Core_Principle.md](Core_Principle.md)
- [Orchestrator_and_Environment.md](Orchestrator_and_Environment.md)
- [Error_and_Feedback_Cycle.md](Error_and_Feedback_Cycle.md)
- [Observability_Framework.md](Observability_Framework.md)
- DOCUMENTATION_MAP.md


## Key Concepts
- To be expanded.
