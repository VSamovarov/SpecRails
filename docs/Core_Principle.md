---
version: 2.3
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-09
---

# ⚙️ Принципы ядра SpecRails

Что такое ядро SpecRails и как оно работает.

---

## 🎯 Главная идея

**SpecRails — это система для превращения текста в структурированные спецификации (DSL).**

**Ядро** — это компоненты, которые **работают с готовым DSL**: проверяют, собирают, управляют версиями.

**Окружение** — это всё, что **помогает создать DSL** (см. [Orchestrator and Environment](Orchestrator_and_Environment.md)).

---

## 🧩 Что такое ядро

**Ядро (Core)** — это набор компонентов, которые:

1. **Проверяют** корректность DSL
2. **Собирают** составные спецификации
3. **Управляют** версиями и схемами

**Ядро работает одинаково**, независимо от:
- Как был создан DSL (AI, человек, шаблон)
- Какой интерфейс показывает результат
- Где и как запущена система

---

## 📦 Компоненты ядра

### 1. Validator (Валидатор)

**Что делает:** Проверяет DSL на корректность

**Вход:** Готовый DSL (YAML/JSON)
```yaml
form:
  name: student_registration
  fields:
    - name: name
      type: string
      required: true
    - name: email
      type: email
      required: true
    - name: birthDate
      type: date
```

**Проверяет:**
- ✅ Синтаксис (валидный YAML/JSON)
- ✅ Структура (соответствие JSON Schema)
- ✅ Типы полей (например, у `type: date` должны быть `min` и `max`)
- ✅ Логические правила (например, `min < max`)

**Результат:**
```json
{
  "valid": true,
  "errors": []
}
```

или

```json
{
  "valid": false,
  "errors": [
    {
      "field": "birthDate",
      "issue": "missing required attribute 'max'"
    }
  ]
}
```

---

### 2. Assembler (Сборщик)

**Что делает:** Собирает составные спецификации

Если одна форма использует компоненты из другой:

```yaml
form:
  name: teacher_profile
  include:
    - common/address_fields
    - common/contact_fields
  fields:
    - name: subject
      type: string
```

Assembler подставляет все `include` и создаёт полную спецификацию.

---

### 3. Schema Registry (Реестр схем)

**Что делает:** Хранит JSON Schema для разных версий DSL

**Зачем:**
- Поддерживает разные версии (v1, v2, v3)
- Проверяет совместимость
- Позволяет откатиться к старой версии

**Пример:**
```yaml
schema_registry:
  - id: spec.form.v1
    file: schemas/form.v1.schema.json
  - id: spec.form.v2
    file: schemas/form.v2.schema.json
    replaces: spec.form.v1
```

---

## ❌ Что НЕ является ядром

### Orchestrator — координатор процесса

**Статус:** Окружение  
**Роль:** Координирует работу всех компонентов (получает зависимости через Dependency Injection)

**Почему НЕ ядро:**
- Зависит от AI и контекста (недетерминированный)
- Разные реализации для разных сред (VS Code, CLI, Web)
- Настраивается извне (не создаёт зависимости сам)

Подробнее: [Orchestrator and Environment](Orchestrator_and_Environment.md)

---

### Parser — преобразователь текста в DSL

**Статус:** Окружение  
**Роль:** Извлекает DSL из сырого ответа AI

**Почему НЕ ядро:**
- Зависит от AI (обрабатывает недетерминированный ответ)
- Ядро может работать **без Parser** — можно писать DSL вручную
- Использует Prompt Registry (окружение)

Подробнее: [Parser Architecture](Parser_Architecture.md)

---

### AI Agent — исполнитель запросов к AI

**Статус:** Окружение  
**Роль:** Вызывает модель AI (GPT, Claude) с промптом

**Почему НЕ ядро:**
- Недетерминированный (AI может давать разные ответы)
- Зависит от внешних сервисов
- Можно заменить (GPT → Claude → Gemini)

---

### Preview Engine — визуализация

**Статус:** Окружение  
**Роль:** Показывает визуальное представление DSL

**Почему НЕ ядро:**
- Не влияет на валидацию
- Только UI слой

---

## 🔄 Как работает ядро

Ядро получает **готовый DSL** и обрабатывает его:

```
DSL (готовый) → Validator → [OK/Error]
                     ↓
                Assembler → Полная спецификация
                     ↓
              Schema Registry ← Проверка версии
```

**Откуда приходит DSL:**
- От Parser (извлёк из ответа AI)
- Написан вручную аналитиком
- Сгенерирован шаблоном

**Ядру всё равно** — оно просто проверяет корректность.

---

## 🧭 Архитектурная диаграмма

```
┌─────────────────────────────────────────────────────────┐
│                  ОКРУЖЕНИЕ (Environment)                │
│                                                         │
│  Orchestrator, Parser, AI Agent, Context Manager        │
│  (см. SpecRails_Orchestrator_and_Environment.md)        │
│                                                         │
│                      ↓ DSL (готовый)                    │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│                     ЯДРО (Core)                         │
│                  Детерминированный слой                 │
│                                                         │
│  ┌──────────┐      ┌───────────┐      ┌───────────┐    │
│  │Validator │─────>│ Assembler │─────>│  Schema   │    │
│  │          │      │           │      │ Registry  │    │
│  └────┬─────┘      └───────────┘      └───────────┘    │
│       │                                                 │
│       │ { valid: true/false, errors: [...] }           │
│       └────────────────────────────────────────────────>│
└─────────────────────────────────────────────────────────┘
```

**Граница:**
- **Окружение** создаёт DSL (недетерминированный процесс)
- **Ядро** работает с готовым DSL (детерминированный процесс)

---

## 🧠 Принципы работы ядра

### 1. Изоляция от AI

**Принцип:** Ядро не зависит от конкретной модели AI и способа создания DSL

**Почему важно:**
- Можно заменить GPT на Claude, Gemini — ядро работает так же
- Можно писать DSL вручную — ядро работает так же
- AI генерирует DSL, но ядро — окончательный судья корректности

---

### 2. Воспроизводимость

**Принцип:** Один и тот же DSL → один и тот же результат

**Почему важно:**
- Гарантия стабильности
- Возможность автоматического тестирования
- Предсказуемость для разработчиков

---

### 3. Прозрачность

**Принцип:** Результат работы ядра всегда понятен человеку

**Почему важно:**
- Можно проверить DSL вручную
- Нет "чёрного ящика"
- Аналитик видит, что именно создано

---

### 4. Расширяемость

**Принцип:** Можно добавлять новые типы DSL через Schema Registry

**Пример:**
```yaml
# Добавляем новый тип - workflow
schema_registry:
  - id: spec.workflow.v1
    file: schemas/workflow.v1.schema.json
```

Ядро автоматически начинает поддерживать новый формат.

---

## ✅ Контрольные критерии

### Что является ядром?

| Вопрос | Должен быть ответ | Пример |
|--------|-------------------|---------|
| Может ли система работать без этого компонента? | Нет | Validator — часть ядра |
| Работает ли компонент с **готовым** DSL? | Да | Assembler — часть ядра |
| Детерминирован ли компонент? | Да | Schema Registry — часть ядра |
| Зависит ли компонент от AI? | Нет | Parser — НЕ часть ядра |
| Можно ли заменить без изменения ядра? | Нет | Validator — часть ядра |

---

### Что НЕ является ядром?

| Вопрос | Должен быть ответ | Пример |
|--------|-------------------|---------|
| Создаёт ли компонент DSL? | Да | Parser — окружение |
| Зависит ли от AI? | Да | AI Agent — окружение |
| Можно ли заменить реализацию? | Да | Orchestrator — окружение |
| Получает ли зависимости извне? | Да | Parser — окружение |

---

## 🔍 Примеры

### ✅ Часть ядра

**Validator** — проверяет DSL  
Без него невозможно гарантировать корректность.

**Schema Registry** — хранит версии схем  
Без него невозможно управлять совместимостью.

---

### ❌ НЕ часть ядра

**Orchestrator** — координирует процесс  
Зависит от AI и контекста, разные реализации для разных сред.

**Parser** — извлекает DSL из текста  
Обрабатывает недетерминированный ответ AI, можно писать DSL вручную.

**AI Agent** — генерирует DSL  
Недетерминирован, можно заменить модель.

**Preview** — показывает визуализацию  
Не влияет на валидацию.

---

## 🎯 Зачем разделять ядро и окружение

### Преимущества

1. **Стабильность**  
   Ядро меняется редко, окружение — часто

2. **Тестируемость**  
   Ядро можно тестировать независимо от AI и UI

3. **Гибкость**  
   Можно менять AI, интерфейс, не трогая ядро

4. **Понятность**  
   Чётко видно, где логика, а где представление

---

## 🔒 Архитектурный вывод

> **SpecRails — это ядро валидации и сборки DSL.**

**Ядро:**
- ✅ Работает с **готовым** DSL
- ✅ Не зависит от AI или способа создания DSL
- ✅ Гарантирует корректность
- ✅ Прозрачно для человека
- ✅ Предсказуемо и воспроизводимо

**Окружение:**
- Создаёт DSL (AI Agent, Parser)
- Координирует процесс (Orchestrator)
- Показывает результат (Preview)
- Подробнее: [Orchestrator and Environment](Orchestrator_and_Environment.md)

Это делает SpecRails **надёжной и предсказуемой системой**, где:
- **Ядро — судья** корректности DSL
- **Окружение — помощник** в создании DSL

---

## 📚 Связанные документы

- [Глоссарий терминов](Terminology_Glossary.md) — определения Validator, Assembler, Schema Registry
- [Orchestrator and Environment](Orchestrator_and_Environment.md) — как координируется работа вокруг ядра
- [Parser Architecture](Parser_Architecture.md) — преобразование текста в DSL
- [Validation Loop](Validation_Loop_Principle.md) — как ядро взаимодействует с окружением
- [Preview Principle](Preview_Principle.md) — роль визуализации
- [Error and Feedback Cycle](Error_and_Feedback_Cycle.md) — обработка ошибок валидации
