---
version: 2.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-08
---

# 📊 Фреймворк наблюдаемости SpecRails

Как SpecRails отслеживает работу AI, контролирует стабильность результатов и обеспечивает прозрачность всех процессов.

---

## 🎯 Зачем нужна наблюдаемость

AI — непредсказуем по природе. Одна и та же инструкция может дать разные результаты. SpecRails решает эту проблему через **наблюдаемость** (observability):

- **Метрики** — числовые показатели работы системы
- **Логи** — записи всех действий
- **Контроль дрейфа** — отслеживание изменений в поведении AI
- **Обратная связь** — учёт комментариев аналитиков

Это превращает непредсказуемый AI в **управляемый инструмент**.

---

## 📋 Что отслеживаем

### 1. Работа AI

| Метрика | Что показывает | Норма |
|---------|----------------|-------|
| **Latency** | Время ответа AI | < 3 сек |
| **Token Usage** | Сколько токенов использовано | Зависит от задачи |
| **Success Rate** | Доля успешных валидаций | > 95% |
| **Retry Count** | Сколько раз пришлось переделывать DSL | < 2 в среднем |

---

### 2. Качество DSL

| Метрика | Что показывает | Норма |
|---------|----------------|-------|
| **Validation Pass Rate** | Процент DSL, прошедших валидацию с первого раза | > 90% |
| **Schema Error Density** | Количество ошибок на 100 генераций | < 5 |
| **Field Completeness** | Все ли обязательные поля заполнены | 100% |

---

### 3. Стабильность системы

| Метрика | Что показывает | Норма |
|---------|----------------|-------|
| **Module Health** | Работают ли все модули корректно | 100% healthy |
| **Sandbox Violations** | Нарушения политики безопасности | 0 |
| **Memory Usage** | Потребление памяти модулями | < 128 MB на модуль |
| **Timeout Rate** | Процент операций, превысивших лимит времени | < 2% |

---

### 4. Обратная связь от людей

| Метрика | Что показывает |
|---------|----------------|
| **Human Feedback Ratio** | Доля результатов, которые пришлось корректировать вручную |
| **Feedback Accept Rate** | Процент успешных исправлений после обратной связи |
| **Correction Types** | Какие типы ошибок чаще всего встречаются |

---

## 🔍 Drift Control — контроль дрейфа

### Что такое дрейф

**Дрейф (drift)** — это постепенное изменение поведения AI при неизменных инструкциях.

**Пример:**
- Вчера AI создавал поле email с типом `string`
- Сегодня начал создавать с типом `text`
- Технически оба варианта корректны, но это **нестабильность**

---

### Как измеряем дрейф

Система сравнивает новые результаты с **эталоном (baseline)**:

```yaml
drift_analysis:
  contract: form.contract.v2
  baseline_date: "2025-10-15"
  current_score: 0.023
  threshold: 0.050
  status: stable  # ✅ score < threshold
```

**Drift Score** — это число от 0 до 1:
- `< 0.05` — стабильно ✅
- `0.05 - 0.15` — требует внимания ⚠️
- `> 0.15` — критический дрейф ❌

---

### Что делаем при дрейфе

| Уровень | Действие |
|---------|----------|
| **Low** (< 0.05) | Ничего, норма |
| **Medium** (0.05-0.15) | Создать предупреждение, запланировать ревью |
| **High** (> 0.15) | Заблокировать контракт, требуется срочное исправление |

**Пример алерта:**

```yaml
drift_alert:
  id: DRIFT-2025-11-08-001
  contract: form.contract.v2
  score: 0.087
  severity: medium
  action: schedule_review
  affected_fields:
    - email (type changed)
    - birthDate (format variation)
```

---

## 📊 Типы собираемых данных

### Метрики (Metrics)

Числовые показатели:
- Время выполнения
- Количество ошибок
- Использование ресурсов

**Пример:**
```json
{
  "timestamp": "2025-11-08T10:30:00Z",
  "module": "form.builder",
  "latency_ms": 842,
  "tokens_used": 1584,
  "validation": "passed",
  "drift_score": 0.012
}
```

---

### Логи (Logs)

Текстовые записи о событиях:

```yaml
log_entry:
  level: info
  timestamp: "2025-11-08T10:30:00Z"
  module: form.builder
  message: "DSL generated successfully"
  contract: form.contract.v2
  user: analyst_viktor
```

---

### Трейсы (Traces)

Полный путь выполнения запроса:

```
Request Start
  ↓
Prompt Execution (1.2s)
  ↓
DSL Generation (0.8s)
  ↓
Validation (0.04s)
  ↓
Preview Render (0.3s)
  ↓
Request Complete (2.34s total)
```

---

### Аудит (Audit Trail)

История изменений:

```yaml
audit_entry:
  id: AUD-2025-11-08-007
  user: analyst_viktor
  action: edit_spec
  file: model.student.v1.yaml
  changes:
    - field: email
      old_value: required:false
      new_value: required:true
  validation: passed
  timestamp: "2025-11-08T10:35:00Z"
```

---

## 🎯 Анализ обратной связи

Система собирает комментарии аналитиков и классифицирует их:

| Тип ошибки | Пример | Автоматическая реакция |
|------------|--------|------------------------|
| **Семантическая** | AI неправильно понял контекст | Уточнить контракт |
| **Форматная** | Некорректный YAML | Переделать с указанием формата |
| **Пропуск полей** | Не хватает обязательного поля | Добавить в контракт требование |
| **Визуальная** | Неудобное расположение в preview | Скорректировать промпт |

**Пример аналитики:**

```yaml
feedback_analytics:
  period: last_7_days
  total_feedbacks: 47
  types:
    semantic: 18 (38%)
    format: 12 (26%)
    missing_field: 11 (23%)
    visual: 6 (13%)
  most_common_fix: "Add field description"
```

---

## 🔔 Система алертов

### Когда создаются алерты

- **Critical Drift** — дрейф > 15%
- **High Error Rate** — ошибки > 10% запросов
- **Sandbox Violation** — нарушение безопасности
- **Timeout Surge** — много превышений лимита времени
- **Feedback Surge** — слишком много ручных правок

---

### Пример алерта

```yaml
alert:
  id: ALR-ERROR-009
  severity: high
  type: validation_errors
  message: "Validation error rate exceeded 10%"
  metrics:
    error_rate: 12.3%
    threshold: 10.0%
  affected_contract: form.contract.v2
  timestamp: "2025-11-08T10:40:00Z"
  action: notify_team
```

---

## 📈 Dashboard — панель наблюдения

### Что показывает

**Real-time Status:**
- Активные задачи
- Очередь запросов
- Текущая нагрузка

**AI Stability:**
- График дрейфа по времени
- Успешность валидаций
- Частота ошибок

**Contracts Health:**
- Активные контракты
- Устаревшие (deprecated)
- Требующие ревью

**Feedback Metrics:**
- Количество правок
- Типы исправлений
- Успешность применения

---

### Пример snapshot

```yaml
dashboard_snapshot:
  timestamp: "2025-11-08T10:45:00Z"
  
  active_tasks: 3
  queue_length: 0
  
  ai_stability:
    avg_drift: 0.024
    trend: stable
    alerts: 0
  
  contracts:
    active: 8
    deprecated: 2
    needs_review: 1
  
  last_24h:
    total_requests: 147
    success_rate: 96.6%
    avg_latency_ms: 1234
    human_corrections: 5
```

---

## 🔧 Интеграция с внешними системами

SpecRails может отправлять данные в:

| Система | Для чего |
|---------|----------|
| **Prometheus** | Хранение и алертинг по метрикам |
| **Grafana** | Визуализация графиков |
| **Elasticsearch** | Анализ логов |
| **Slack / Teams** | Уведомления о критических событиях |
| **Sentry** | Отслеживание ошибок |

---

## 📦 Хранение данных

### Политика retention (хранения)

```yaml
data_retention:
  telemetry_metrics: 30 days
  audit_logs: 90 days
  health_history: 14 days
  drift_baselines: 180 days
  anonymize_user_data: true
```

### Где хранится

```
.specrails/
  ├── telemetry/      # Метрики
  ├── audit/          # Аудит-логи
  ├── baselines/      # Эталоны для drift control
  └── snapshots/      # Снимки состояния системы
```

---

## ✅ Преимущества наблюдаемости

| Преимущество | Результат |
|--------------|-----------|
| **Прозрачность** | Видно, что происходит в системе в любой момент |
| **Контроль** | Дрейф AI отслеживается и предотвращается |
| **Диагностика** | Быстрое обнаружение и исправление проблем |
| **Обучение** | Система улучшается на основе истории |
| **Доверие** | Аналитики видят, что процесс управляем |

---

## 🧭 Архитектурный вывод

> **Наблюдаемость — это не просто мониторинг, а контроль качества AI.**

SpecRails не может сделать AI абсолютно предсказуемым, но может:

1. **Измерить** его поведение через метрики
2. **Отследить** изменения через drift control
3. **Исправить** проблемы через обратную связь
4. **Доказать** надёжность через аудит

Это превращает AI из чёрного ящика в **прозрачный и управляемый инструмент**.

---

## 📚 Связанные документы

- [Глоссарий терминов](Terminology_Glossary.md) — определения Telemetry, Drift, Observability
- [Validation Loop](Validation_Loop_Principle.md) — как валидация связана с метриками
- [Prompt Registry](Prompt_Registry_and_Runtime_Manifest.md) — связь контрактов и drift control
