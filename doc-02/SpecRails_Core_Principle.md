---
version: 2.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-09
---

# ⚙️ Принципы ядра SpecRails

Что такое ядро SpecRails и как оно работает.

---

## 🎯 Главная идея

**SpecRails — это система для превращения текста в структурированные спецификации (DSL).**

Всё остальное — AI, preview, интеграции — это **окружение**, которое помогает ядру работать надёжно и удобно.

---

## 🧩 Что такое ядро

**Ядро (Core)** — это набор компонентов, которые:

1. **Преобразуют** текст в DSL
2. **Проверяют** корректность DSL
3. **Собирают** составные спецификации
4. **Управляют** версиями и схемами

**Ядро работает одинаково**, независимо от:
- Какой AI используется
- Какой интерфейс показывает результат
- Где и как запущена система

---

## 📦 Компоненты ядра

### 1. Parser (Парсер)

**Что делает:** Преобразует текст в структурированный DSL

**Вход:** Текстовое описание от аналитика или AI
```
Создай форму регистрации студента с полями:
- Имя (обязательное)
- Email (обязательное)
- Дата рождения
```

**Выход:** YAML/JSON структура
```yaml
form:
  name: student_registration
  fields:
    - name: name
      type: string
      required: true
    - name: email
      type: email
      required: true
    - name: birthDate
      type: date
```

---

### 2. Validator (Валидатор)

**Что делает:** Проверяет DSL на корректность

**Проверяет:**
- ✅ Синтаксис (валидный YAML/JSON)
- ✅ Структура (соответствие JSON Schema)
- ✅ Типы полей (например, у `type: date` должны быть `min` и `max`)
- ✅ Логические правила (например, `min < max`)

**Результат:**
```json
{
  "valid": true,
  "errors": []
}
```

или

```json
{
  "valid": false,
  "errors": [
    {
      "field": "birthDate",
      "issue": "missing required attribute 'max'"
    }
  ]
}
```

---

### 3. Assembler (Сборщик)

**Что делает:** Собирает составные спецификации

Если одна форма использует компоненты из другой:

```yaml
form:
  name: teacher_profile
  include:
    - common/address_fields
    - common/contact_fields
  fields:
    - name: subject
      type: string
```

Assembler подставляет все `include` и создаёт полную спецификацию.

---

### 4. Schema Registry (Реестр схем)

**Что делает:** Хранит JSON Schema для разных версий DSL

**Зачем:**
- Поддерживает разные версии (v1, v2, v3)
- Проверяет совместимость
- Позволяет откатиться к старой версии

**Пример:**
```yaml
schema_registry:
  - id: spec.form.v1
    file: schemas/form.v1.schema.json
  - id: spec.form.v2
    file: schemas/form.v2.schema.json
    replaces: spec.form.v1
```

---

## ❌ Что НЕ является ядром

### AI Layer (Слой AI)

**Статус:** Окружение  
**Роль:** Генерирует текст и предложения, но **не источник истины**

Ядро может работать **без AI** — можно писать DSL вручную.

---

### Preview Engine (Движок превью)

**Статус:** Окружение  
**Роль:** Показывает визуальное представление DSL

Preview помогает аналитику проверить результат, но **не влияет на валидацию**.

---

### VS Code Extension (Расширение)

**Статус:** Интерфейс  
**Роль:** Точка входа для пользователя

Ядро может работать через **CLI, API, web-интерфейс** — расширение только один из вариантов.

---

### UI Renderer (Рендерер интерфейса)

**Статус:** Плагин  
**Роль:** Превращает DSL в HTML/Vue/React компоненты

Ядро создаёт DSL, но **не отвечает** за то, как он выглядит в браузере.

---

## 🔄 Как работает ядро

```
Текст → Parser → DSL → Validator → [OK/Error]
                              ↓
                         Assembler → Полная спецификация
                              ↓
                       Schema Registry ← Проверка версии
```

---

## 🧠 Принципы работы ядра

### 1. Изоляция от AI

**Принцип:** Ядро не зависит от конкретной модели AI

**Почему важно:**
- Можно заменить GPT на Claude, Gemini — ядро работает так же
- AI генерирует DSL, но ядро — окончательный судья корректности

---

### 2. Воспроизводимость

**Принцип:** Один и тот же DSL → один и тот же результат

**Почему важно:**
- Гарантия стабильности
- Возможность автоматического тестирования
- Предсказуемость для разработчиков

---

### 3. Прозрачность

**Принцип:** Результат работы ядра всегда понятен человеку

**Почему важно:**
- Можно проверить DSL вручную
- Нет "чёрного ящика"
- Аналитик видит, что именно создано

---

### 4. Расширяемость

**Принцип:** Можно добавлять новые типы DSL через Schema Registry

**Пример:**
```yaml
# Добавляем новый тип - workflow
schema_registry:
  - id: spec.workflow.v1
    file: schemas/workflow.v1.schema.json
```

Ядро автоматически начинает поддерживать новый формат.

---

## ✅ Контрольные критерии ядра

Вопросы для проверки, что компонент относится к ядру:

| Вопрос | Должен быть ответ | Пример |
|--------|-------------------|---------|
| Может ли ядро работать без этого компонента? | Нет | Parser — часть ядра |
| Зависит ли корректность DSL от этого компонента? | Да | Validator — часть ядра |
| Можно ли заменить этот компонент без изменения ядра? | Нет | Schema Registry — часть ядра |
| Влияет ли этот компонент на визуализацию? | Нет | Preview — НЕ часть ядра |

---

## 🔍 Примеры

### ✅ Часть ядра

**Validator** — проверяет DSL  
Без него невозможно гарантировать корректность.

**Schema Registry** — хранит версии схем  
Без него невозможно управлять совместимостью.

---

### ❌ НЕ часть ядра

**AI Agent** — генерирует DSL  
Можно писать DSL вручную, ядро работает так же.

**Preview Renderer** — показывает форму  
Можно использовать DSL без превью, например, в CI/CD.

---

## 🧭 Архитектурная диаграмма

```
┌─────────────────────────────────────────┐
│            ОКРУЖЕНИЕ                    │
│                                         │
│  ┌──────────┐        ┌──────────┐       │
│  │ AI Agent │        │ Preview  │       │
│  └────┬─────┘        └────▲─────┘       │
│       │                   │             │
└───────┼───────────────────┼─────────────┘
        │                   │
        ▼                   │
┌─────────────────────────────────────────┐
│             ЯДРО (CORE)                 │
│                                         │
│  ┌────────┐   ┌──────────┐   ┌───────┐ │
│  │ Parser │──>│Validator │──>│Output │ │
│  └────────┘   └──────────┘   └───────┘ │
│       │             │                   │
│       ▼             ▼                   │
│  ┌─────────────────────────────┐       │
│  │    Schema Registry          │       │
│  └─────────────────────────────┘       │
│                                         │
└─────────────────────────────────────────┘
```

---

## 🎯 Зачем разделять ядро и окружение

### Преимущества

1. **Стабильность**  
   Ядро меняется редко, окружение — часто

2. **Тестируемость**  
   Ядро можно тестировать независимо от AI и UI

3. **Гибкость**  
   Можно менять AI, интерфейс, не трогая ядро

4. **Понятность**  
   Чётко видно, где логика, а где представление

---

## 🔒 Архитектурный вывод

> **SpecRails — это ядро преобразования текста в DSL.**

Ядро:
- ✅ Не зависит от AI
- ✅ Работает одинаково везде
- ✅ Гарантирует корректность
- ✅ Прозрачно для человека

Окружение (AI, Preview, UI):
- Помогает создавать DSL удобнее
- Делает результат понятнее
- Ускоряет работу аналитика
- Но **не влияет на корректность**

Это делает SpecRails **надёжной и предсказуемой системой**, где AI — помощник, а не хозяин.

---

## 📚 Связанные документы

- [Глоссарий терминов](SpecRails_Terminology_Glossary.md) — определения Parser, Validator, Schema
- [Validation Loop](SpecRails_Validation_Loop_Principle.md) — как ядро взаимодействует с AI
- [Preview Principle](SpecRails_Preview_Principle.md) — роль визуализации
