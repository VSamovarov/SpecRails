---
version: 2.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-08
---

# 🧩 Реестр промптов и манифест выполнения

Как SpecRails управляет промптами, модулями и их версиями в процессе работы.

---

## 🎯 Зачем это нужно

В SpecRails два важных вопроса:

1. **Какие промпты использовать** для генерации DSL?
2. **Какие модули загружены** в системе?

Ответы на эти вопросы даёт **единый реестр**, который объединяет:

- **Prompt Registry** — хранилище всех промптов с версиями и контрактами
- **Runtime Manifest** — описание модулей, которые активны в данный момент

Это обеспечивает **воспроизводимость** (один и тот же промпт даёт тот же результат) и **безопасность** (только проверенные модули могут работать).

---

## 📋 Prompt Registry — реестр промптов

### Что это

Централизованное хранилище всех промптов, которые AI использует для генерации DSL.

### Что хранится

Каждый промпт в реестре имеет:

- **ID и версию** — например, `form.contract.v2`
- **Контракт** — описание того, что AI должен вернуть (JSON Schema)
- **Зависимости** — какие другие промпты нужны для работы
- **Статус** — active, deprecated, archived
- **Историю** — кто и когда создал/изменил

### Пример записи в реестре

```yaml
registry:
  - id: form.contract.v2
    status: active
    contract_file: "contracts/form.contract.yaml"
    schema: "schemas/form.dsl.schema.json"
    depends_on:
      - base.contract.v1
      - model.contract.v1
    created_at: "2025-10-15"
    owner: "specrails-core"

  - id: model.contract.v1
    status: deprecated
    replaced_by: model.contract.v2
    deprecated_since: "2025-11-01"
```

---

### Жизненный цикл промпта

```
Draft → Review → Published → Active → Deprecated → Archived
```

| Стадия | Что происходит |
|--------|----------------|
| **Draft** | Промпт создан, но ещё не проверен |
| **Review** | Проходит ревью архитектором или QA |
| **Published** | Добавлен в реестр, получил версию |
| **Active** | Используется для генерации DSL |
| **Deprecated** | Устарел, но ещё поддерживается |
| **Archived** | Снят с эксплуатации, хранится в истории |

---

### Версионирование промптов

Версии следуют правилам [Semantic Versioning](https://semver.org/):

| Изменение | Версия | Пример | Что изменилось |
|-----------|--------|--------|----------------|
| **Major** | `v1 → v2` | `form.contract.v2` | Несовместимая структура ответа |
| **Minor** | `v1.0 → v1.1` | `form.contract.v1.1` | Добавлены необязательные поля |
| **Patch** | `v1.1.0 → v1.1.1` | `form.contract.v1.1.1` | Исправление ошибок без изменения логики |

Все версии хранятся в реестре. Можно вернуться к старой версии через `rollback`.

---

### Совместимость и зависимости

Если промпт зависит от другого, система проверяет их совместимость:

```yaml
form.contract.v2:
  depends_on:
    - base.contract.v1  # ✅ активен
    - model.contract.v3 # ❌ не найден
```

Если зависимость не найдена или несовместима — промпт не будет использован.

---

## ⚙️ Runtime Manifest — манифест выполнения

### Что это

Описание того, какие модули и утилиты загружены в SpecRails в данный момент.

### Что включает

```yaml
runtime_manifest:
  version: 1.2.0
  build_date: "2025-11-08"
  environment: vscode-plugin
  
  modules:
    - name: core.runtime
      version: 1.0.0
      status: active
    
    - name: form.builder
      version: 0.4.2
      type: utility
      contract: form.contract.v2
      enabled: true
    
    - name: model.designer
      version: 0.2.1
      type: utility
      enabled: false  # отключен
  
  contracts:
    - id: form.contract.v2
      schema: schemas/form.dsl.schema.json
      checksum: 8a2f9bcd...
```

---

### Типы модулей

| Тип | Описание | Пример |
|-----|----------|---------|
| **Core** | Базовые компоненты ядра | `core.runtime`, `validator` |
| **Utility** | Вспомогательные функции | `form.builder`, `model.designer` |
| **Extension** | Расширения функциональности | `analytics`, `export.pdf` |
| **Renderer** | Визуализация специфичных форматов | `preview.html`, `preview.vue` |

---

### Регистрация модуля

Модули регистрируются через SDK:

```javascript
import { registerModule } from '@specrails/sdk'

registerModule({
  name: 'form.builder',
  type: 'utility',
  version: '0.4.2',
  dependencies: ['@specrails/core'],
  
  setup(core) {
    // Регистрируем утилиту
    core.registerUtility('form.builder', async (input) => {
      // Выполняем промпт
      const result = await core.PromptAPI.execute({
        contract: 'form.contract.v2',
        input
      })
      
      // Валидируем результат
      return core.ValidatorAPI.validate(result.dsl)
    })
  }
})
```

---

### Проверка зависимостей

При загрузке модуль проверяется на наличие зависимостей:

```yaml
form.builder:
  requires:
    - name: core.runtime
      version: ">=1.0.0"  # ✅ установлен 1.0.0
    - name: validator
      version: "^2.5.0"   # ❌ установлен 2.3.0
```

Если зависимость не соответствует — модуль не загружается, система выдаёт ошибку.

---

## 🔐 Безопасность — Sandbox и Integrity

### Sandbox Policy (Песочница)

Каждый модуль выполняется в изолированной среде с ограниченным доступом:

```yaml
sandbox_policy:
  allowed_apis:
    - PromptAPI     # ✅ можно
    - ValidatorAPI  # ✅ можно
    - ContextAPI    # ✅ только чтение
  
  restricted:
    - fs            # ❌ нельзя читать файлы
    - http          # ❌ нельзя делать сетевые запросы
    - child_process # ❌ нельзя запускать процессы
  
  limits:
    timeout_ms: 5000
    memory_mb: 128
```

Если модуль нарушает правила — он блокируется и помещается в **quarantine** (карантин).

---

### Integrity Verification (Проверка целостности)

Каждый модуль имеет контрольную сумму (SHA256):

```yaml
integrity_check:
  module: form.builder
  version: 0.4.2
  checksum: ffae1c27d...
  verified: true
  timestamp: "2025-11-08T10:00:00Z"
```

Если контрольная сумма не совпадает — модуль блокируется до ручного подтверждения.

---

## 🔄 Как это работает вместе

### Процесс загрузки

```
1. Система стартует
   ↓
2. Читает Runtime Manifest
   ↓
3. Проверяет зависимости модулей
   ↓
4. Проверяет целостность (checksums)
   ↓
5. Загружает Core модули
   ↓
6. Загружает Utilities и Extensions
   ↓
7. Регистрирует Contracts из Prompt Registry
   ↓
8. Система готова к работе
```

---

### Процесс выполнения промпта

```
1. Analyst даёт задачу
   ↓
2. Система находит нужный contract в Registry
   ↓
3. Проверяет версию и зависимости
   ↓
4. Загружает нужный модуль из Manifest
   ↓
5. Модуль выполняет промпт через PromptAPI
   ↓
6. AI возвращает DSL
   ↓
7. Validator проверяет результат
   ↓
8. DSL передаётся в Preview
```

---

## 📊 Телеметрия и мониторинг

Система собирает метрики:

| Метрика | Что показывает |
|---------|----------------|
| **Active Modules** | Количество загруженных модулей |
| **Sandbox Violations** | Нарушения политики безопасности |
| **Dependency Conflicts** | Конфликты версий |
| **Load Latency** | Время загрузки модулей |
| **Integrity Failures** | Ошибки проверки контрольных сумм |

Пример snapshot:

```yaml
registry_snapshot:
  timestamp: "2025-11-08T10:15:00Z"
  active_modules: 
    - core.runtime
    - form.builder
  quarantined: []
  disabled: 
    - model.designer
  integrity_status: ok
  dependency_health: stable
```

---

## 🔁 Аудит и трассировка

Каждое использование промпта записывается в **Audit Trail**:

```yaml
audit_entry:
  timestamp: "2025-11-08T10:20:00Z"
  contract_used: form.contract.v2
  ai_model: gpt-5
  input_hash: a7f3bc...
  result_hash: 8ffbc1a...
  validation: passed
  analyst: user@example.com
```

Это позволяет:
- Понять, какой промпт создал конкретный DSL
- Воспроизвести результат с теми же условиями
- Найти причину ошибок в прошлом

---

## ✅ Преимущества единого реестра

| Преимущество | Результат |
|--------------|-----------|
| **Воспроизводимость** | Один промпт → один результат (при тех же условиях) |
| **Безопасность** | Только проверенные модули могут работать |
| **Прозрачность** | Видно, какие модули загружены и какие промпты используются |
| **Управляемость** | Легко обновить или откатить версию |
| **Трассируемость** | Полная история использования промптов |

---

## 🧭 Архитектурный вывод

> **Prompt Registry + Runtime Manifest = источник истины о состоянии системы.**

SpecRails не просто выполняет промпты — она **управляет** ими:
- Знает, какие версии активны
- Проверяет зависимости и совместимость
- Контролирует безопасность модулей
- Сохраняет историю всех действий

Это превращает хаотичное использование AI в **инженерно управляемый процесс**.

---

## 📚 Связанные документы

- [Глоссарий терминов](SpecRails_Terminology_Glossary.md) — определения Contract, Manifest, Registry
- [Validation Loop](SpecRails_Validation_Loop_Principle.md) — как промпты используются в цикле валидации
- [Security Model](SpecRails_Security_Model.md) — детали Sandbox Policy
