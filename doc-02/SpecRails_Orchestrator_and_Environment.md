version: 1.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-09
 
# 🌍 Orchestrator и окружение — принципиальный обзор

Документ описывает архитектурную роль Orchestrator и окружения SpecRails на уровне рядового разработчика. Код и интерфейсы намеренно опущены — фокус на ответственности, потоках и границах. Язык технический, но без избыточного усложнения.

---

## 🎯 Суть

Окружение отвечает за получение DSL из пользовательского ввода (частично недетерминированный процесс). Ядро оперирует уже сформированным DSL и выполняет детерминированные проверки и операции. Orchestrator связывает эти два мира, управляя последовательностью шагов и обеспечивая контролируемый переход от «сырого намерения» к проверяемому артефакту.

Ключевой принцип разделения: генерация подвержена вариативности (AI, контекст), проверка — стабильна и воспроизводима.

---

## 🧩 Состав окружения
Окружение формирует рабочий контур генерации DSL и включает типовые роли:
1. Orchestrator — управляет сценарием выполнения (workflow orchestration).
2. Parser — интерпретирует ответ модели и извлекает структурированный DSL.
3. AI Agent — единая точка обращения к внешней модели.
4. Context Manager — поставляет исторические и пользовательские параметры.
5. Preview Engine — визуализация полученного DSL.
6. Error Recovery — стратегии повторной попытки и коррекции при сбоях.

Все элементы обслуживают конвейер «ввод → генерация → структурирование». Ядро получает только конечный DSL и диагностические данные.

---

## 🎭 Роль Orchestrator

Orchestrator выполняет функцию координатора процесса:
– принимает задачу и параметры исполнения;
– агрегирует контекст (история, предпочтения пользователя, последние артефакты);
– инициирует цепочку генерации: вызов модели → парсинг → формирование DSL;
– передаёт результат ядру для проверки;
– обрабатывает ветвление по ошибкам (повтор, коррекция, возврат сообщения);
– регистрирует итог и обновляет контекст.

Он не содержит жёстко прошитых реализаций зависимостей — они внедряются извне. Это даёт возможность составлять разные конфигурации для IDE, CLI, Web без изменения логики координации.

---

## 🛑 Почему Orchestrator вне ядра
– Оперирует недетерминированным материалом (ответ AI, вариативность контекста).
– Зависит от пользовательского состояния и сессии; ядро работает на статичных входах.
– Требует адаптации под разные рантаймы (editor extension, CLI runner, web backend).
– Эволюционирует чаще: добавление стратегий генерации, изменяемые политики retries.

Ядро остаётся изолированным: не знает ни о модели, ни о механике получения исходного DSL. Это удерживает детерминированный слой чистым.

---

## 🏗 Application Layer
Внешний слой отвечает за составление графа зависимостей и инициализацию окружения: выбирает реализации (какой AI провайдер, какой формат хранения контекста), подготавливает конфигурацию и передаёт Orchestrator готовый набор. После инициализации его главное действие — делегировать обработку задач.

Принцип: создание экземпляров происходит вне бизнес-логики компонентов; они остаются пассивными.

---

## 🔄 Высокоуровневый поток
1. Ввод задачи (натуральный текст / параметры).
2. Агрегация контекста (история + предпочтения).
3. Генерация ответа AI (вариативный шаг).
4. Парсинг и извлечение DSL.
5. Проверка ядром (валидатор, схемы, правила).
6. Ветвление: успех → визуализация; ошибка → стратегии восстановления / возврат.
7. Обновление контекста (при необходимости) и логирование.

Цель: минимизировать число недетерминированных точек и сделать их наблюдаемыми.

---

## 🔧 Компоненты и обязанности
– Orchestrator: orchestration / контроль потока.
– Parser: семантическое извлечение и структурирование.
– AI Agent: абстракция вызова модели (uniform API).
– Context Manager: управление состоянием сеанса, предпочтениями.
– Preview Engine: представление DSL для быстрой обратной связи.
– Error Recovery: обработка проблемных сценариев (retry, downgrade, сообщение).
– Validator (ядро): формальная проверка соответствия контрактам/схемам.

Каждый компонент имеет единственную зону ответственности (single responsibility), скрытых побочных действий быть не должно.

---

## 🧠 Архитектурные принципы
1. Чёткая граница генерация/проверка.
2. Централизованная координация (single orchestration point).
3. Переиспользуемость за счёт внедрения зависимостей (pluggable components).
4. Прозрачность ошибок и состояния (observable pipeline).
5. Пассивные компоненты — создание вне их ответственности.
6. Минимизация связности между окружением и ядром (loose coupling).
7. Трассируемость шагов (traceable transformations).

---

## 🧷 Инварианты
– Ядро никогда не инициирует вызовы моделей.
– Окружение не выполняет финальную доменную валидацию — только приводит данные.
– Orchestrator не содержит жёстко закодированных реализаций.
– Ошибки поднимаются вверх без скрытого подавления.
– Все шаги воспроизводимы и логируемы.

---

## 📐 Качества
– Гибкость: смена AI или форматирования не затрагивает ядро.
– Тестируемость: изолированное покрытие компонентов mock‑зависимостями.
– Надёжность: явная обработка отказов и деградаций.
– Масштабируемость: добавление новых контрактов/стратегий без переписывания основ.
– Поддерживаемость: ясная декомпозиция и трассировка.

---

## ⚠ Сценарии отказов
– Некорректный/неполный ответ модели → формирование частичного/пустого DSL + диагностический блок.
– Отсутствующий контракт → возврат контролируемой ошибки конфигурации.
– Провал проверки в ядре → инициирование стратегии восстановления (повтор / корректировка / остановка).
– Истечение лимита повторов → возврат итогового набора ошибок пользователю.
– Повреждённый контекст → переход к безопасному минимальному состоянию.

Результат всегда: валидный DSL или структурированное сообщение о проблеме.

---

## 🔒 Итог
Окружение — адаптивный слой генерации и преобразования. Ядро — детерминированный слой проверки и применения правил. Orchestrator — мост, обеспечивающий управляемый и наблюдаемый переход между ними. Такая сегрегация повышает устойчивость и ускоряет эволюцию без риска нарушить предсказуемость.

---

## 🔗 Связанные документы
– `SpecRails_Core_Principle.md`
– `SpecRails_Parser_Architecture.md`
– `SpecRails_Prompt_Registry_and_Runtime_Manifest.md`
– `SpecRails_Validation_Loop_Principle.md`
– `SpecRails_Error_and_Feedback_Cycle.md`

---

## 📝 Примечание
Все примеры кода, интерфейсы и детали реализации убраны намеренно. Документ фиксирует только смысл и принципы.
