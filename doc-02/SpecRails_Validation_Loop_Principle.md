---
version: 2.0
status: draft
reviewed_by: AI Agent
last_updated: 2025-11-08
---

# 🔁 Принцип цикла валидации SpecRails

Как SpecRails обеспечивает качество спецификаций через двухконтурную валидацию и обратную связь.

---

## 🎯 Суть

SpecRails проверяет корректность DSL двумя способами:

1. **Машинная валидация (Validator)** — автоматическая проверка структуры и правил
2. **Человеческая валидация (Analyst)** — визуальная проверка смысла через preview

Вместе они создают цикл **«AI → Validator → Preview → Analyst → Feedback → AI»**, который повторяется до тех пор, пока спецификация не станет корректной и понятной.

---

## ⚙️ Машинная валидация

### Что проверяет Validator

- ✅ **Синтаксис** — корректный YAML/JSON
- ✅ **Структура** — соответствие JSON Schema
- ✅ **Типы полей** — правильность атрибутов (например, у `type: date` должны быть `min` и `max`)
- ✅ **Сборка компонентов** — корректная работа `include`, `component`, `use`
- ✅ **Нормализация** — добавление метаданных, устранение двусмысленностей

### Как работает

1. AI создаёт черновой DSL
2. Validator проверяет его
3. Если есть ошибки — формирует отчёт
4. AI получает отчёт и исправляет DSL
5. Повторяется до успеха

### Пример отчёта об ошибках

```json
{
  "valid": false,
  "errors": [
    {
      "field": "email",
      "issue": "отсутствует обязательный атрибут 'required'",
      "severity": "error"
    },
    {
      "field": "birthDate",
      "issue": "неверный тип, должен быть 'date'",
      "severity": "error"
    }
  ]
}
```

AI видит этот отчёт и пересоздаёт DSL с учётом ошибок.

---

## 👁️ Человеческая валидация

### Зачем нужна

Validator проверяет **формальную** корректность, но не понимает **смысл**. Например:

- Поля расположены в неудобном порядке
- Текст подсказки непонятен пользователю
- Логика формы не соответствует бизнес-процессу

Это проверяет аналитик через preview.

### Как работает

1. Validator подтверждает, что DSL корректен
2. Preview отображает форму визуально
3. Аналитик смотрит и оценивает
4. Если нужны изменения — даёт обратную связь AI
5. AI обновляет DSL
6. Цикл повторяется

### Пример обратной связи

> **Аналитик:** «Поле 'Дата рождения' должно быть обязательным и находиться выше поля 'Email'»

AI понимает комментарий, обновляет DSL, Validator проверяет, Preview показывает новый вариант.

---

## 🔄 Полный цикл валидации

```
┌──────────────────────────────────────────────────────┐
│                     Analyst                          │
│ (даёт описание задачи или обратную связь)            │
└─────────────────┬────────────────────────────────────┘
                  │
                  ▼
         ┌────────────────┐
         │   AI Agent     │
         │  (создаёт DSL) │
         └────────┬───────┘
                  │
                  ▼
         ┌────────────────────┐
         │   Validator        │
         │ (проверяет DSL)    │
         └────────┬───────────┘
                  │
         ┌────────┴───────────┐
         │                    │
    [✅ Valid]          [❌ Errors]
         │                    │
         ▼                    ▼
   ┌──────────┐      ┌──────────────┐
   │ Preview  │      │ Error Report │
   └────┬─────┘      └──────┬───────┘
        │                   │
        ▼                   │
   ┌──────────┐             │
   │ Analyst  │◄────────────┘
   │ (review) │
   └────┬─────┘
        │
   [OK] │ [Fix needed]
        │      │
        ▼      ▼
      [Done] [Feedback to AI] ──┐
                                 │
                                 └──► (цикл повторяется)
```

---

## 🧠 Роли в цикле

### AI Agent
**Задача:** Превращать текст в DSL

**Возможности:**
- Понимает естественный язык
- Знает структуру DSL
- Учится на ошибках

**Ограничения:**
- Не всегда точен
- Нуждается в валидации
- Работает только через контракты

---

### Validator (Core)
**Задача:** Проверять формальную корректность

**Возможности:**
- Быстрая автоматическая проверка
- Точное определение ошибок
- Генерация понятных отчётов

**Ограничения:**
- Не понимает смысл
- Не исправляет ошибки сам (только сообщает)

---

### Analyst (Человек)
**Задача:** Проверять смысловую корректность

**Возможности:**
- Понимает бизнес-логику
- Видит UX-проблемы
- Принимает финальное решение

**Ограничения:**
- Не может проверять вручную большие объёмы
- Нуждается в визуальном preview

---

## 📋 Принципы работы

| Принцип | Описание |
|---------|----------|
| **AI создаёт** | Генерирует DSL из текста, но не является источником истины |
| **Validator проверяет** | Гарантирует структурную корректность |
| **Analyst утверждает** | Подтверждает, что спецификация соответствует ожиданиям |
| **Итеративность** | Ошибки не блокируют, а запускают новый цикл |
| **Прозрачность** | Каждый шаг виден и понятен |

---

## 🔁 Обработка ошибок

### Типы ошибок

| Тип | Кто находит | Кто исправляет | Пример |
|-----|-------------|----------------|---------|
| **Синтаксис** | Parser | AI (автоматически) | Невалидный YAML |
| **Структура** | Validator | AI (по отчёту) | Неверный тип поля |
| **Логика** | Validator | AI + подсказка | Некорректная комбинация атрибутов |
| **Смысл** | Analyst | AI (по обратной связи) | Поле не соответствует бизнес-логике |

---

### Цикл исправления

1. Validator обнаруживает ошибку
2. Формирует структурированный отчёт
3. AI получает отчёт в понятном формате
4. AI пересоздаёт DSL с учётом ошибок
5. Validator проверяет снова
6. Если ошибок нет → переход к preview
7. Если ошибки остались → повтор с уточнением

---

### Пример диалога с AI

**Validator:**
```json
{
  "errors": [
    {"field": "email", "issue": "отсутствует атрибут 'required'"}
  ]
}
```

**Feedback для AI:**
```
Ты создал DSL с ошибкой:
- Поле 'email' должно иметь атрибут 'required: true'

Пожалуйста, исправь DSL согласно схеме spec.form.v1
```

**AI:** *(пересоздаёт DSL с исправлением)*

---

## ✅ Преимущества двухконтурной валидации

| Преимущество | Результат |
|--------------|-----------|
| **Надёжность** | Все DSL проходят строгую проверку |
| **Прозрачность** | Аналитик видит процесс, а не чёрный ящик |
| **Качество** | Ошибки ловятся до этапа разработки |
| **Обучаемость** | AI улучшается на основе истории ошибок |
| **Воспроизводимость** | Одинаковый вход → одинаковый результат |

---

## 🧭 Что даёт эта модель

### Для AI
- Чёткие правила и границы
- Конкретная обратная связь об ошибках
- Возможность самокоррекции

### Для Analyst
- Контроль над результатом
- Визуальная проверка без чтения YAML
- Простой способ дать обратную связь

### Для системы в целом
- Стабильность и предсказуемость
- Защита от некорректных спецификаций
- Накопление знаний через историю ошибок

---

## 🔒 Архитектурный вывод

> **Качество спецификации — это не одноразовая проверка, а итеративный процесс.**

SpecRails не пытается создать идеальный DSL с первого раза. Вместо этого система:

1. Даёт AI попробовать
2. Проверяет результат машинно
3. Показывает аналитику визуально
4. Собирает обратную связь
5. Повторяет цикл до успеха

Такой подход превращает неточность AI в управляемый, прозрачный и улучшаемый процесс.

---

## 📚 Связанные документы

- [Глоссарий терминов](SpecRails_Terminology_Glossary.md) — определения Validator, Contract, Feedback
- [Core Principle](SpecRails_Core_Principle.md) — что входит в ядро SpecRails
- [Error and Feedback Cycle](SpecRails_Error_and_Feedback_Cycle.md) — детали обработки ошибок
