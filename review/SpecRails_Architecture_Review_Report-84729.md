---
version: 1.0
status: active
reviewed_by: AI Architectural Reviewer
date: 2025-11-11
focus: High-level architectural principles
scope: Core, Process, Governance layers
---

# 🧠 SpecRails Architecture Review Report

## 1. Общая оценка

### Философская согласованность: ✅ Сильная
Принцип **"AI как инструмент, человек как смысловой валидатор"** прослеживается в глоссарии:
- AI Agent явно позиционируется как генератор, но не источник истины
- Analyst имеет финальное слово через Preview и Feedback
- Validator изолирован от исправлений (проверяет, не корректирует)

### Модульная изоляция: ⚠️ Средняя с критическими пробелами
**Обнаружены проблемы:**
1. **Parser ↔ Prompt Registry** — отсутствует формализованная граница
2. **Validation Loop ↔ Orchestrator** — концептуальное описание без операционной модели
3. **Governance ↔ Observability** — административная изоляция, нет feedback hook

### Риски:
🔴 **Высокий**: Размытая ответственность между Parser и Manifest может привести к дублированию логики  
🟠 **Средний**: Validation Loop не операционализирован — риск несогласованной реализации  
🟡 **Низкий**: Терминология устарела, но не критично

---

## 2. Несогласованности

| Документ                                       | Проблема                                                                | Тип                            | Предложение                                                                              |
|------------------------------------------------|-------------------------------------------------------------------------|--------------------------------|------------------------------------------------------------------------------------------|
| `Terminology_Glossary.md` → `Parser`           | Нет указания на границу с Prompt Registry                               | Архитектурный пробел           | Добавить термин **PromptManifestDTO** как контракт между Parser и Registry               |
| `Terminology_Glossary.md` → `Runtime Manifest` | Описан как "для модуля", но используется и Parser'ом                    | Неясная область применения     | Разделить на **Module Manifest** и **Prompt Manifest**                                   |
| `Validation Loop`                              | Отсутствует фаза Preview как UX-валидация                               | Процессный пробел              | Явно включить **Preview Phase** в цикл: `AI → Validator → Preview → Analyst`             |
| `Orchestrator`                                 | Нет описания механизма передачи управления между агентами               | Отсутствие операционной модели | Определить **Agent State Machine** с событиями (parsed, validated, reviewed, approved)   |
| `Governance ↔ Observability`                   | Односторонняя связь: Observability собирает, но не влияет на Governance | Разорванный feedback loop      | Ввести **Policy Hook** — механизм обновления правил на основе метрик                     |

---

## 3. Отсутствующие связи

### 🔴 Критично отсутствует: Parser ↔ Prompt Registry интерфейс

**Текущее состояние:**
```yaml
Parser:
  role: "преобразует текст в DSL"
  output: "YAML/JSON структура"
  
Prompt Registry:
  role: "хранилище промптов с версиями"
  content: "промпты, контракты, зависимости"
```

**Проблема:** Не описано:
- Как Parser регистрирует сгенерированный промпт в Registry?
- Кто отвечает за валидацию структуры манифеста?
- Есть ли версионирование при изменениях?

**Предлагаемое решение:**
```yaml
# Новый контракт: PromptManifestDTO
PromptManifestDTO:
  name: string
  version: semver
  contract: JSON_Schema_Reference
  dependencies: [PromptManifest]
  metadata:
    author: string
    created_at: timestamp
    status: [draft, active, deprecated]
```

**Принцип изоляции:**
- Parser создаёт DTO, но НЕ регистрирует
- Prompt Registry принимает DTO и управляет жизненным циклом
- Validator проверяет DTO на соответствие схеме

---

### 🟠 Средняя критичность: Validation Loop без операционной модели

**Текущее описание:**
> AI → Validator → Preview → Analyst → обратная связь → AI (повтор до готовности)

**Отсутствует:**
1. Как определяется "готовность"?
2. Кто принимает решение о переходе между фазами?
3. Где хранится состояние цикла?

**Предлагаемая схема:**
```mermaid
statechart
  [*] --> Generating
  Generating --> Validating : Parser.complete
  Validating --> Invalid : Validator.fail
  Validating --> Previewing : Validator.pass
  Previewing --> Reviewing : Preview.ready
  Reviewing --> Approved : Analyst.approve
  Reviewing --> Feedback : Analyst.reject
  Feedback --> Generating : Feedback.apply
  Invalid --> Generating : Error.fix
  Approved --> [*]
```

**Необходимо добавить:**
- `ValidationState` enum (generating, validating, previewing, reviewing, approved, rejected)
- `TransitionRules` — кто может переводить между состояниями
- `StateStorage` — где хранится текущее состояние (in-memory, persistent)

---

### 🟡 Низкая критичность: Governance ← Observability feedback отсутствует

**Текущее:**
```
Observability собирает метрики → Dashboard → человек анализирует → ручное изменение политик
```

**Проблема:** Нет автоматического механизма влияния метрик на правила.

**Предложение: Policy Hook**
```yaml
PolicyHook:
  trigger:
    metric: "drift_index"
    threshold: 0.15
  action:
    type: "require_manual_review"
    target: "all_changes_to_contract"
  
  trigger:
    metric: "validation_failure_rate"
    threshold: 0.3
    window: "24h"
  action:
    type: "disable_auto_merge"
    notify: ["tech_lead", "architect"]
```

---

## 4. Противоречия терминов

### ✅ Консистентность высокая
Глоссарий демонстрирует строгое разграничение:
- **DSL** vs **Config** — используется корректно
- **Contract** vs **Prompt Template** — чётко разделено
- **Feedback** vs **Comment** — не смешивается

### ⚠️ Устаревшие термины

| Термин отсутствует | Где используется | Предложение |
|-------------------|------------------|-------------|
| **Manifest Schema** | Parser, Prompt Registry | Добавить как формализованное описание структуры манифеста |
| **Policy Hook** | Governance, Observability | Ввести для автоматизации реакции на метрики |
| **Sandbox Context** | Runtime, Extensibility | Уточнить как подмножество Context с ограничениями |
| **Agent State** | Validation Loop, Orchestrator | Описать жизненный цикл AI-агента в процессе валидации |
| **Manifest Lifecycle** | Core Principle (упоминается в Improvement Plan) | Отсутствует в глоссарии, но критично для понимания |

---

## 5. Рекомендации

### 🎯 Немедленные действия (2-4 недели)

#### 1. Формализовать интерфейс Parser ↔ Prompt Registry
**Создать:**
- `PromptManifestDTO.json` — JSON Schema
- Секцию в `Terminology_Glossary.md` → "Manifest Lifecycle"
- Диаграмму взаимодействия в `Parser_Architecture.md`

**Принцип:**
```
Parser создаёт структуру → возвращает DTO → Registry валидирует и регистрирует
```

#### 2. Операционализировать Validation Loop
**Добавить в `Validation_Loop_Principle.md`:**
- Enum состояний `ValidationState`
- Правила переходов `TransitionRules`
- Диаграмму состояний (как выше)
- Роль Preview как обязательной UX-фазы

**Обновить `Orchestrator_and_Environment.md`:**
- Описать Agent Roles (Parser, Validator, Analyst)
- Добавить Event Bus для передачи управления
- Определить Execution Context для каждой фазы

#### 3. Замкнуть контур Governance ↔ Observability
**Создать механизм Policy Hook:**
- Добавить в `Governance_Policy_Matrix.md` секцию "Automated Policy Triggers"
- В `Observability_Framework.md` описать "Governance Feedback Metrics"
- В `Error_and_Feedback_Cycle.md` включить "Governance Hooks"

**Схема:**
```
Observability (метрики) → Policy Hook (условия) → Governance (действия) → Validation Loop (применение)
```

---

### 🔄 Стратегические улучшения (1-3 месяца)

#### 4. Расширить глоссарий новыми терминами
**Добавить секции:**
- **Manifest Lifecycle** (draft → registered → active → deprecated)
- **Policy Hook** (триггеры, действия, уведомления)
- **Agent State** (роли, состояния, переходы)
- **Sandbox Context** (subset of Context с security constraints)

#### 5. Создать Reference Architecture
**Новый документ:** `docs/Reference_Architecture.md`

**Содержание:**
- Все интерфейсы между слоями (Core ↔ Process ↔ Governance)
- Canonical Data Flow (от user story до deployed DSL)
- Security Boundaries (что может и не может пересекать модуль)
- Extension Points (где разработчики могут добавлять функциональность)

#### 6. Интегрировать CI/CD в Governance
**Обновить `CICD_Quick_Start.md`:**
- Добавить проверку Manifest Consistency
- Интегрировать Drift Index в pipeline
- Настроить автоматический reject при превышении порогов

---

## 6. Уровень зрелости архитектуры

### Оценка: **3.5 / 5** (Defined → Managed)

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| **Философская согласованность** | 5/5 | Принцип "AI as tool" чётко прослеживается |
| **Модульное разделение** | 3/5 | Концептуально верно, но границы не формализованы |
| **Архитектурные ограничения** | 4/5 | Изоляция ядра описана, Sandbox присутствует |
| **Целостность документации** | 3/5 | Глоссарий полный, но связи между документами неявные |
| **Операционализация** | 2/5 | Validation Loop и Orchestrator концептуальны, без исполнительной модели |
| **Feedback Loop** | 3/5 | Есть идея, но не замкнут (Governance не реагирует на Observability) |
| **Терминология** | 4/5 | Строгая и последовательная, но не покрывает новые концепции |

### Текущий уровень: **Defined**
> Архитектурные принципы задокументированы, но их реализация не стандартизирована

### Следующий уровень: **Managed**
> Для достижения необходимо:
> 1. Формализовать все интерфейсы (DTO, API)
> 2. Операционализировать процессы (state machines, event bus)
> 3. Замкнуть контуры обратной связи (policy hooks)

---

## 📊 Матрица приоритетов

```
┌─────────────────────────────────────────────────────────────┐
│ Влияние на архитектуру                                       │
│                                                               │
│   High │ 🔴 Parser↔Registry  │ 🔴 Validation Loop           │
│        │    interface        │    state machine             │
│        ├────────────────────┼──────────────────────────────┤
│        │ 🟠 Governance↔Obs   │ 🟠 Manifest Lifecycle       │
│ Medium │    feedback hook    │    documentation             │
│        ├────────────────────┼──────────────────────────────┤
│   Low  │ 🟡 Glossary updates │ 🟡 CI/CD integration        │
│        │    new terms        │    governance checks         │
└─────────────────────────────────────────────────────────────┘
         Low                    Medium                    High
                    Усилия на реализацию
```

---

## ✅ Заключение

### Сильные стороны:
1. ✅ Философия проекта чётко определена и последовательна
2. ✅ Терминология строгая и согласованная
3. ✅ Принцип изоляции ядра продекларирован
4. ✅ Validation Loop концептуально верен

### Критические пробелы:
1. ❌ Отсутствует формализованный интерфейс Parser ↔ Prompt Registry
2. ❌ Validation Loop не операционализирован (нет state machine)
3. ❌ Governance и Observability не замкнуты в feedback loop

### Рекомендация:
**Начать с формализации интерфейсов (PromptManifestDTO)** — это фундамент для дальнейших улучшений. Без чёткой границы между Parser и Registry невозможно корректно реализовать Orchestrator и Governance механизмы.

**Приоритет действий:**
1. Создать `PromptManifestDTO` схему
2. Описать Validation Loop как state machine
3. Добавить Policy Hook в Governance
4. Обновить глоссарий новыми терминами

---

**Дата ревью:** 2025-11-11  
**Проведено:** AI Architectural Reviewer  
**Методология:** High-level architectural analysis  
**Следующий ревью:** После реализации критических рекомендаций
