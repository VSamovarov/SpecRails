# Инструкции для AI-агента SpecRails

## 🎯 Цель проекта
Основная цель SpecRails — преобразование неструктурированного текста от аналитика в структурированную, версионируемую и валидированную спецификацию на DSL (предметно-ориентированном языке). Ключевая философия — «контроль важнее магии», что гарантирует, что ИИ действует как предсказуемая утилита в инженерном рабочем процессе.

## 🏗️ Базовая архитектура: 3-слойная модель
Система разделена на три отдельных слоя с однонаправленными зависимостями (`Core` ← `Process` ← `Governance`). Канонический обзор архитектуры находится в `docs/Reference_Architecture.md`.

1.  **Слой Core (Неизменяемая основа)**
    *   **Ответственность:** Валидация, хранение и управление схемами. Он работает с **уже сгенерированным** DSL.
    *   **Компоненты:** `Validator`, `Schema Registry`, `Prompt Registry`.
    *   **Ключевой принцип:** Слой Core полностью изолирован и не имеет информации о слоях `Process` или `Governance`. Он не взаимодействует с внешними моделями ИИ. Это детерминированный, подобный чистой функции слой.
    *   **Ключевые файлы:** `docs/Core_Principle.md`.

2.  **Слой Process (Динамическое рабочее пространство)**
    *   **Ответственность:** Генерация DSL из текста, координация жизненного цикла валидации и управление циклами взаимодействия с пользователем. Здесь происходит «грязная» работа по взаимодействию с ИИ.
    *   **Компоненты:** `Parser Agent`, `Orchestrator`, `Validation Loop`.
    *   **Ключевой принцип:** Этот слой оркеструет агентов и процессы. `Parser Agent` — ЕДИНСТВЕННЫЙ компонент, который взаимодействует с внешней моделью ИИ для генерации DSL.
    *   **Ключевые файлы:** `docs/Parser_Architecture.md`, `docs/Orchestrator_and_Environment.md`.

3.  **Слой Governance (Контролирующий слой)**
    *   **Ответственность:** Мониторинг качества системы, применение политик и проведение аудитов. Он реагирует на события и метрики из других слоев.
    *   **Компоненты:** `Policy Matrix`, `Observability`.
    *   **Ключевые файлы:** `docs/Governance_Policy_Matrix.md`.

## 🔄 Ключевой поток данных: Создание промпта
Понимание этого потока критически важно для большинства задач.

1.  **Аналитик** предоставляет неструктурированный текст.
2.  **`Parser Agent`** (слой Process) принимает текст, взаимодействует с ИИ и генерирует черновик `PromptManifestDTO`.
3.  `PromptManifestDTO` передается в **`Validator`** (слой Core).
4.  `Validator` использует схемы из **`Schema Registry`** (слой Core) для проверки структурной корректности.
5.  Если DTO валиден, манифест регистрируется в **`Prompt Registry`** (слой Core).

## 📜 Взаимодействие на основе контрактов
- Обмен данными между слоями осуществляется строго через Объекты Передачи Данных (DTO).
- Основным контрактом является `PromptManifestDTO`. Его JSON Schema — источник истины.
- **Расположение схем:** `schemas/PromptManifestDTO.json`. Перед изменением любых структур данных необходимо проверить, требуется ли обновление схемы.

##  Рабочие процессы и соглашения для разработчиков
- **pnpm Workspace:** Проект представляет собой монорепозиторий, использующий `pnpm` workspaces. Пакеты находятся в (пока не существующей) директории `packages/`.
- **Документация — это главное:** Директория `docs/` — это не просто документация, а **источник истины для архитектуры**. Любое значительное изменение в логике или ответственности **должно** быть сначала отражено в этих markdown-файлах.
- **Язык:** Документация проекта и, вероятно, внутренние комментарии написаны на **русском языке**. Учитывайте это при поиске и внесении изменений.
